# PRP — P6.M1.T1.S3: Update `docs/examples.md` + `docs/troubleshooting.md` with host-rules content

> **Repo under change:** the **qmkonnect** desktop app at
> `/home/dustin/projects/qmkonnect`. This is a **documentation-only** task
> (item CONTRACT: "DOCS: This IS the documentation subtask"). It edits **TWO**
> Markdown files — `docs/examples.md` (8.0 KB) and `docs/troubleshooting.md`
> (17.3 KB) — by adding the host-side-rules (`rules.toml`) **recipes + FAQ
> entries**.
>
> **Files touched (2):** `docs/examples.md`, `docs/troubleshooting.md`.
> **Nothing else** — no Rust, no Cargo, no other docs, no `llms_full.txt`
> (regenerated by P6.M1.T1.S4), no spec.
>
> **The feature is SHIPPED.** Per the live `plan_status`, **P1–P5 are all
> Complete**: the typed-command transport + firmware, the host pattern matcher,
> the `rules.toml` data model + evaluator, the handshake + send logic, the CLI
> flags (`--list-callbacks` / `--validate-rules` / `--rules-path`), and the tray
> **"Reload rules"** item on **all three platforms** (verified in `src/main.rs`,
> `src/tray.rs`, `src/linux_tray.rs`). This task documents a real, landed feature
> — the troubleshooting entries cite the **actual shipped CLI messages + exit
> codes** (read from `src/main.rs::list_callbacks` / `validate_rules`, see
> research §0.4), not a spec prediction. There are no code-blocker hedges.
>
> **This task is the how-to/troubleshooting layer of the host-rules docs.**
> S1 (`docs/configuration.md`, COMPLETE) owns the **schema REFERENCE** (field
> table, the verbatim §9 example, CLI flag table, file location). S2
> (`docs/qmk-integration.md`, READY) owns the **migration GUIDE** (the 4-step
> procedure, `DEFINE_HOST_CALLBACKS` explanation). **S3 (this) owns the RECIPES**
> (a concrete worked `rules.toml` example + a firmware→host before/after) and
> **the 4 troubleshooting entries** named in the item CONTRACT. S3 **links out**
> to S1/S2; it does NOT reproduce their content.

---

## Goal

**Feature Goal**: Make `docs/examples.md` the home of a **concrete, worked
host-rules recipe** (a reader who has read Examples 1–3's firmware rules should,
in Example 4, see the *same kind* of app→layer / app→callback behavior expressed
as an editable `rules.toml` — including a firmware→host before/after and a plain
runtime walkthrough of the stack-vs-replace model); and make
`docs/troubleshooting.md` cover the **four host-rules failure modes** named in the
item CONTRACT (legacy firmware, unknown callback names, `rules.toml` parse errors,
and "connected but rules not applying"), each with the real, shipped diagnostic
commands (`--list-callbacks`, `--validate-rules`) and their exact messages +
exit codes, and cross-links to the schema (S1) and migration (S2) pages.

**Deliverable** (two files edited):
1. **`docs/examples.md`** — a new `## Example 4: Host-Side Rules (rules.toml)`
   section (inserted after Example 3, before `## Pattern Matching Tips`)
   containing: a scenario blockquote, a firmware "before" block (the one-time
   `DEFINE_HOST_CALLBACKS` registry + the `DEFINE_SERIAL_LAYERS`/
   `DEFINE_SERIAL_COMMANDS` rows being migrated — reusing Example 2's
   gaming/office callback names), the equivalent `rules.toml` "after" block
   (layer rules incl. a class-only and a class+title form; callback rules; one
   `disable_firmware_config = true` replace rule), a "What happens at runtime"
   walkthrough (a STACK window, a REPLACE window, a callback-only window), and a
   before/after mapping note that links to `/configuration` (schema) and
   `/qmk-integration` (the full migration procedure). Plus an **optional** one-line
   addition to the intro blockquote flagging Example 4 as the host-side alternative.
2. **`docs/troubleshooting.md`** — a new `## Host Rules Issues` H2 section
   (inserted between `## Configuration Issues` and `## Performance Issues`) with
   four `### ` entries — *Legacy firmware (proto_ver != 2) — host rules disabled*,
   *Callback name not found in registry*, *rules.toml parse error*, and *Device
   shows connected but rules not applying* — each in the file's existing
   `**Symptoms**` / numbered `**Solutions**` / ```bash-block format, citing the
   **shipped CLI messages + exit codes** and linking out to `/configuration`
   and/or `/qmk-integration`.

**Success Definition**:
- Both edits render cleanly under Jekyll (kramdown + rouge; ```c / ```toml /
  ```bash fences balanced file-wide; no malformed `{{ site.baseurl }}` links).
- Example 4's `rules.toml` is a **different** scenario from the §9 reference
  (`alacritty`/`neovide`/`*chrome*` — owned verbatim by S1 in configuration.md):
  it reuses Example 2's gaming/office context (`steam_app*`, `cs2`, `*word*`,
  `enable_gaming`/`disable_gaming`) and uses host layers **≥ 224** (firmware
  `_GAMING = 1` → host `layer = 224`). `grep -c alacritty examples.md == 0`.
- Example 4 demonstrates **both** stack (the `[host] disable_firmware_config =
  false` default) and replace (an explicit `disable_firmware_config = true`
  rule), and the runtime walkthrough states the **per-window AND** decision
  correctly (one non-disabling matched rule ⇒ the window stacks).
- The firmware "before" block uses the canonical `DEFINE_HOST_CALLBACKS` row shape
  `{ name, on_enable, on_disable }` (matching the firmware README `mute` form),
  and notes `on_disable` may be `NULL` and the pattern is NOT a registry field.
- The 4 troubleshooting entries cite the **shipped** CLI behavior verbatim
  (research §0.4): `--list-callbacks` legacy line `Legacy firmware (no callback
  support) — host rules will run in string-only mode.` (exit 0); `--validate-rules`
  prints `rules.toml invalid: {e}` and exits **non-zero** on parse/schema error
  (and on a missing explicit `--rules-path`), but prints `⚠  unknown callback:
  {name}` as a **warning (exit 0)** for unknown names; no `rules.toml` ⇒ `No
  rules.toml found (host rules disabled). Nothing to validate.` (exit 0).
- The troubleshooting entries point at the tray **"Reload rules"** item (which
  ships on all three platforms — no platform hedge).
- `git diff --stat` = `docs/examples.md` + `docs/troubleshooting.md` ONLY.

## User Persona (if applicable)

**Target User**: a power user who has integrated the `qmk-notifier` firmware,
written `DEFINE_SERIAL_*` rules (Examples 1–3), and now wants to (a) see a
**concrete `rules.toml` example** they can adapt, and (b) **diagnose** why their
host rules aren't working.

**Use Case** (examples.md): "I read Examples 1–3 and want host rules — but I want
to *see* a complete, realistic `rules.toml` before I write my own, including how
to take a firmware rule I already have and express it on the host. I open Example
4, see my gaming/offfice scenario ported to `rules.toml` with a before/after, copy
it, adapt it, validate it, and reload."

**Use Case** (troubleshooting.md): "My host rules aren't switching layers. I open
Troubleshooting → Host Rules Issues, find 'Device shows connected but rules not
applying', run `qmkonnect --validate-rules` and `--list-callbacks`, and follow the
checklist (rules.toml present + valid? proto_ver 2? pattern matches the real
class? reloaded? layer ≥ 224?)."

**Pain Points Addressed**: today both files are **100% firmware-side**. A user who
discovers host rules (via configuration.md's schema from S1, or the tray "Reload
rules") has (a) no concrete worked `rules.toml` example to copy, and (b) no FAQ
for the four ways host rules silently fail (legacy firmware, typo'd callback
names, a TOML parse error, or a non-matching pattern).

## Why

- **Item CONTRACT point 3 (LOGIC)** names exactly what to add: in examples.md "a
  complete rules.toml example showing layer rules, callback rules,
  disable_firmware_config, and the stack/replace model" + "the before/after
  migration from `DEFINE_*`"; in troubleshooting.md the four named entries.
- **HOST_RULES.md §4 + §9** are the canonical stack/replace model + example;
  **§8(6)** is the CLI contract; the **firmware README** is the canonical
  `DEFINE_HOST_CALLBACKS` form. S3 turns those into user-facing recipes + FAQs,
  and — because the code is shipped — the FAQ cites the real `src/main.rs` output.
- **Complements** S1 (configuration.md = the **reference**; S3 = the **recipe**)
  and S2 (qmk-integration.md = the **procedure**; S3 = the **worked example**).
  S1's configuration.md links to `/examples` for "ready-made recipes"; S2's
  qmk-integration.md links to `/examples` too. S3 is the target of those links —
  it must contain real recipes.
- Host rules have **four distinct silent-failure modes** (legacy firmware, unknown
  callback names, parse errors, non-matching patterns) that today's
  troubleshooting.md — which only covers firmware/string-path issues — does not
  address at all.

## What

**Two additive edits**, one per file. The existing sections in both files stay
**verbatim** (the only optional existing-content touch is a single additive line
in examples.md's intro blockquote). Everything else is **inserted**, not modified.

1. **`docs/examples.md`** — INSERT `## Example 4: Host-Side Rules (rules.toml)`
   after Example 3 (after its `**Result**:` line, L229) and before
   `## Pattern Matching Tips` (L231). Optionally append one line to the L9 intro
   blockquote flagging Example 4.
2. **`docs/troubleshooting.md`** — INSERT `## Host Rules Issues` (4 `### `
   entries) between the end of `## Configuration Issues` (~L455) and
   `## Performance Issues` (L456).

### Success Criteria
- [ ] examples.md has a new `## Example 4: Host-Side Rules (rules.toml)` section
      after Example 3, before `## Pattern Matching Tips`.
- [ ] Example 4 contains a firmware **"before"** block (```c) showing the one-time
      `DEFINE_HOST_CALLBACKS({ … })` registry (canonical `{ name, on_enable,
      on_disable }` row shape) **and** the `DEFINE_SERIAL_LAYERS` /
      `DEFINE_SERIAL_COMMANDS` rows being migrated (reusing Example 2's
      `steam_app*` / `cs2` / `*word*` / `enable_gaming_mode` /
      `disable_gaming_mode` names).
- [ ] Example 4 contains a `rules.toml` **"after"** block (```toml) that is a
      **DIFFERENT scenario from §9** (no `alacritty`/`neovide`), demonstrates a
      class-only `match` (`"steam_app*"`), a class+title `match`
      (`["cs2", "Counter-Strike 2"]`), a `[[callback_rules]]` `enable`, and at
      least one `disable_firmware_config = true` (replace) rule.
- [ ] Example 4's `rules.toml` uses host layers **≥ 224** (e.g. `layer = 224`,
      `layer = 225`) — NOT the firmware examples' low layers (0/1/2/3); the
      translation is called out.
- [ ] Example 4 has a "What happens at runtime" paragraph walking ≥2 windows:
      a **STACK** window (board rules run + host layer on top), a **REPLACE**
      window (board rules skipped via `clear_board`), stating the **per-window
      AND** decision (one non-disabling matched rule ⇒ stack).
- [ ] Example 4 links to `{{ site.baseurl }}/configuration` (full schema + CLI
      flags) and `{{ site.baseurl }}/qmk-integration` (the 4-step migration
      procedure).
- [ ] troubleshooting.md has a new `## Host Rules Issues` section between
      `## Configuration Issues` and `## Performance Issues`, with FOUR `### `
      entries matching the CONTRACT names: *Legacy firmware
      (proto_ver != 2) — host rules disabled*; *Callback name not found in
      registry*; *rules.toml parse error*; *Device shows connected but rules not
      applying*.
- [ ] Each troubleshooting entry cites the **shipped CLI behavior verbatim**
      (research §0.4): `--list-callbacks` legacy line (exit 0);
      `--validate-rules` `rules.toml invalid: {e}` (non-zero exit) on parse error
      and `⚠  unknown callback: {name}` (warning, exit 0) on unknown names;
      `No rules.toml found … Nothing to validate.` (exit 0) when absent.
- [ ] The "Reload rules" advice points at the tray **"Reload rules"** item with
      **no platform hedge** (it ships on macOS/Windows + Linux SNI).
- [ ] ```c / ```toml / ```bash fences are balanced (even ``` line count) in BOTH
      files.
- [ ] All new internal links use `{{ site.baseurl }}/slug` (no `.md`, no absolute
      URLs for internal pages).
- [ ] `git diff --stat` = `docs/examples.md` + `docs/troubleshooting.md` ONLY.

## All Needed Context

### Context Completeness Check

_Pass_: an agent with no prior knowledge of this codebase can implement this
using only this PRP + `research/notes.md`, because: (a) the exact current H2
skeleton + insertion points of **both** files (line-accurate) are in research §1.1
/ §1.2; (b) the verbatim item CONTRACT (the 2 file edits + the 4 troubleshooting
entries) is in research §0.1; (c) the canonical stack/replace model + matcher
parity vocabulary + `DEFINE_HOST_CALLBACKS` row shape are in research §0.2 / §4
(from `HOST_RULES.md` §4/§8/§9/§10 + the firmware README); (d) the **shipped** CLI
messages + exit codes (read from `src/main.rs`) are tabulated verbatim in research
§0.4, and the tray "Reload rules" cross-platform presence is in §0.5; (e) the
canonical callback names to reuse for page continuity
(`enable_gaming_mode`/`disable_gaming_mode` from Example 2) are in research §0.3;
(f) the boundary with S1/S2/S4 (link out, don't duplicate) is in research §2;
(g) design decisions D1-D8 and gotchas G1-G8 are in research §3/§4; (h) the
validation commands are in research §5. No code/build dependency.

### Documentation & References

```yaml
# MUST READ — THIS task's full contract (verbatim item CONTRACT, spec sources,
# the SHIPPED CLI message/exit-code table, current skeletons of BOTH files,
# boundary, validation).
- file: plan/002_637d65b6e9b8/P6M1T1S3/research/notes.md
  why: "§0.1 = the verbatim item CONTRACT (2 file edits + the 4 FAQ entries).
        §0.2 = the canonical spec sources (HOST_RULES.md §4 stack/replace model,
        §8(2) matcher parity, §9 the reference example to NOT duplicate, §8(6) CLI
        contract, §10 migration). §0.3 = canonical callback names to reuse
        (enable_gaming_mode/disable_gaming_mode from Example 2). §0.4 = the SHIPPED
        CLI messages + exit codes to cite VERBATIM in the FAQ (read from
        src/main.rs::list_callbacks / validate_rules). §0.5 = the tray 'Reload
        rules' item ships on ALL three platforms. §1 = line-accurate skeletons +
        insertion points of BOTH files. §2 = boundary (S1=configuration schema/CLI,
        S2=qmk-integration migration, S4=llms_full.txt regen — do NOT duplicate).
        §3/§4 = D1-D8 design + G1-G8 gotchas. §5 = validation."

# MUST READ — the shipped CLI behavior (cite its messages verbatim in the FAQ).
- file: src/main.rs
  why: "fn list_callbacks (≈L261) prints 'No QMK device connected…' / 'Connected
        keyboard reports 0 callbacks.' / 'Callback name -> id (N):' + '  {id:>3}
        {name}' rows / 'Legacy firmware (no callback support) — host rules will
        run in string-only mode.' (all exit 0). fn validate_rules (≈L305) prints
        'rules file not found: {p}' (non-zero) on a missing explicit --rules-path;
        'No rules.toml found (host rules disabled). Nothing to validate.' (exit 0)
        when no candidate; 'Validating {path}'; 'rules.toml invalid: {e}'
        (non-zero) on parse/schema error; '⚠  unknown callback: {name}' (warning,
        exit 0) per unknown name; 'rules.toml valid: {N} layer rules, {M} callback
        rules.' on success. The troubleshooting entries quote these EXACTLY."
  section: "fn list_callbacks, fn validate_rules (the --list-callbacks /
            --validate-rules dispatch ≈L125-164; the two fn bodies ≈L261-355)"
  gotcha: "P5.M1.T1.S1 is COMPLETE — this code is shipped. Cite the real strings;
           do NOT paraphrase (users grep for them)."

# MUST READ — the canonical spec (stack/replace model, matcher parity, CLI contract).
- file: spec/HOST_RULES.md
  why: "§4 = the stack-vs-replace coexistence model (the per-window AND semantics
        — the source for Example 4's runtime walkthrough). §8(2) = Pattern::Single
        (class-only) vs Pattern::Parts (class+title == WT) — the matcher semantics
        the recipes rely on. §8(6) = the CLI contract (the spec the shipped code
        implements). §9 = the reference example to NOT duplicate (it's in
        configuration.md via S1). §10 = the migration procedure (owned by S2;
        Example 4 shows a worked before/after + links out). §3 C7/C10/C11 =
        no-match-clear, per-rule disable_firmware_config, host layers >= 224."
  section: "§4 (stack/replace), §8(2) (matcher), §8(6) (CLI), §9 (DON'T duplicate),
            §10 (migration → S2)"

# MUST READ — the canonical firmware README (DEFINE_HOST_CALLBACKS form + vocabulary).
- file: /home/dustin/projects/qmk-notifier/README.md
  why: "§'Host-Side Rules & Typed Commands' has the canonical DEFINE_HOST_CALLBACKS
        `mute` example ({ name, on_enable, on_disable }; on_disable may be NULL),
        the 'two independent state planes' model, the 'Stack vs replace per window'
        prose (clear_board=0 stack / =1 replace), and the backward-compat guarantee.
        Example 4's 'before' block + the FAQ wording mirror this vocabulary."
  section: "'Host-Side Rules & Typed Commands' (example, how-it-works, stack/replace,
            backward compat)"

# MUST READ — the files THIS task edits (the ONLY two files).
- file: docs/examples.md
  why: "8.0 KB Jekyll page. The new '## Example 4' section is INSERTED after
        Example 3's '**Result**:' line (L229) and before '## Pattern Matching Tips'
        (L231). Example 2 (L85-160) is the Gaming & Productivity scenario whose
        callback names (enable_gaming_mode/disable_gaming_mode) + apps
        (steam_app*, cs2, *word*, *excel*) Example 4 REUSES for continuity. The L9
        intro blockquote says 'QMKonnect only sends the active-window string' —
        append ONE line flagging Example 4 as the host-side alternative (optional)."
  pattern: "front matter (permalink: /examples/); ```c fences; `[text]({{ site.baseurl }}/page)` links;
            blockquote intros; '**Scenario**:' / '**Result**:' structure per example."
  gotcha: "do NOT edit Examples 1-3, Pattern Matching Tips, Testing, or Next Steps beyond
           the optional intro-line + the inserted Example 4. do NOT paste the §9
           alacritty/neovide reference (S1 owns it in configuration.md). do NOT
           regenerate llms_full.txt (S4)."

- file: docs/troubleshooting.md
  why: "17.3 KB Jekyll page. The new '## Host Rules Issues' H2 is INSERTED between
        the end of '## Configuration Issues' (~L455, after '### Wrong Keyboard IDs'
        / '#### Using System Tools') and '## Performance Issues' (L456). Each entry
        follows the file's existing '**Symptoms**: …' / numbered '**Solutions**:' /
        ```bash-block format (see e.g. '### Keyboard Not Detected', '### Invalid
        Configuration File')."
  pattern: "front matter (permalink: /troubleshooting/); ```bash fences for commands;
            '**Symptoms**:' / '**Solutions**:' / numbered lists; cross-links via
            {{ site.baseurl }}/slug (existing: /examples, /installation, /qmk-integration)."
  gotcha: "do NOT edit existing sections; insert the new H2 only. New entries must cite
           the SHIPPED CLI messages verbatim (research §0.4). The tray 'Reload rules'
           item ships on all platforms — no hedge."

# MUST READ — the sibling PRPs (treat as contracts; LINK to their pages, don't duplicate).
- file: plan/002_637d65b6e9b8/P6M1T1S1/PRP.md
  why: "S1's configuration.md owns: the rules.toml schema FIELD TABLE, the verbatim
        §9 annotated example, the 3 CLI flags TABLE, the per-OS file location, and
        the disable_firmware_config field-level semantics. S1 LINKS OUT to /examples
        for 'ready-made recipes'. This task (S3) is the target — it must contain
        real recipes and LINK BACK to /configuration for the schema/CLI reference.
        Assume S1 lands as specified; do not duplicate its schema/CLI content."
  section: "Goal + What (the configuration.md section shape) + Cross-links (S1 → /examples)"

- file: plan/002_637d65b6e9b8/P6M1T1S2/PRP.md
  why: "S2's qmk-integration.md owns: the host-rules MIGRATION guide — the 4-step
        procedure (expose callbacks → layer rule → callback rule → iterate) and the
        DEFINE_HOST_CALLBACKS registry explanation. S2 LINKS OUT to /examples and
        /troubleshooting. This task (S3) shows a worked before/after RECIPE (not the
        procedure) and LINKS to /qmk-integration for the procedure. Assume S2 lands
        as specified; do not duplicate its 4-step procedure."
  section: "Goal + What (the qmk-integration.md migration section) + Cross-links"
```

### Current Codebase tree (relevant subset)

```bash
docs/
  examples.md          # ← THIS TASK EDITS (file 1 of 2): + "## Example 4: Host-Side
                       #   Rules (rules.toml)" (scenario + firmware "before" + rules.toml
                       #   "after" + runtime walkthrough + before/after mapping), +
                       #   cross-links to /configuration + /qmk-integration. Optional:
                       #   one line in the intro blockquote. Jekyll page (kramdown/rouge).
  troubleshooting.md   # ← THIS TASK EDITS (file 2 of 2): + "## Host Rules Issues" H2
                       #   (4 ### entries: legacy firmware / callback name not found /
                       #   rules.toml parse error / connected but rules not applying),
                       #   + cross-links to /configuration + /qmk-integration.
  configuration.md     # P6.M1.T1.S1 COMPLETE (rules.toml schema + CLI flags). UNCHANGED (linked OUT).
  qmk-integration.md   # P6.M1.T1.S2 READY (host-rules migration guide). UNCHANGED (linked OUT).
  README.md            # repo README. P6.M1.T1.S4. UNCHANGED.
  llms_full.txt        # GENERATED concatenation of docs/*.md. P6.M1.T1.S4 regenerates
                       #   it AFTER all doc edits land. NOT touched by this task.
  _config.yml          # Jekyll: baseurl "/qmkonnect", markdown kramdown, highlighter rouge.
src/
  main.rs              # P5.M1.T1.S1 LANDED — fn list_callbacks / validate_rules. The FAQ
                       #   cites its exact stdout/stderr + exit codes (READ-ONLY source).
  tray.rs              # P5.M2.T1.S1 LANDED — "Reload rules" MenuItem (macOS/Windows).
  linux_tray.rs        # P5.M2.T1.S2 LANDED — "Reload rules" SNI item (Linux).
spec/
  HOST_RULES.md        # §4 stack/replace + §8(2) matcher + §8(6) CLI + §9 ref (don't dup) + §10 migration. READ-ONLY.
# (external, READ-ONLY) the authoritative firmware README:
/home/dustin/projects/qmk-notifier/README.md   # "Host-Side Rules & Typed Commands" — the
                       # canonical DEFINE_HOST_CALLBACKS example + stack/replace prose.
```

### Desired Codebase tree with files to be changed

```bash
docs/examples.md         # MODIFIED: + "## Example 4: Host-Side Rules (rules.toml)" section
                         #   (scenario blockquote + firmware "before" ```c block w/
                         #   DEFINE_HOST_CALLBACKS + DEFINE_SERIAL_* rows + rules.toml
                         #   "after" ```toml block w/ layer_rules + callback_rules +
                         #   disable_firmware_config true/false + runtime stack/replace
                         #   walkthrough + before/after note), + cross-links. Optional
                         #   intro-blockquote line.
docs/troubleshooting.md  # MODIFIED: + "## Host Rules Issues" H2 section (4 ### entries
                         #   matching the CONTRACT names, each Symptoms/Solutions +
                         #   ```bash diagnostic commands + verbatim shipped CLI messages
                         #   + exit codes + cross-links to /configuration + /qmk-integration).
# EVERYTHING ELSE UNCHANGED. No Rust, no Cargo.toml, no other docs, no llms_full.txt, no spec.
```

### Known Gotchas of our codebase & Library Quirks

```markdown
<!-- G1 (this is NOT a code task; the feature is SHIPPED). P1-P5 are all Complete —
     the CLI flags, handshake, and tray "Reload rules" item are landed. This task
     edits two MARKDOWN files; it does NOT compile or run Rust. There are no build
     preconditions to gate on. Cite the real src/main.rs behavior (research §0.4). -->

<!-- G2 (do NOT paste the §9 example or the field table). S1 owns those in
     configuration.md. Example 4's rules.toml recipe must be a DIFFERENT block
     (the gaming/office scenario — see research §3 D2). Cite schema facts inline
     (e.g. "layer must be >= 224") with a link to /configuration. The §9 marker is
     "alacritty" — examples.md must contain ZERO occurrences of it. -->

<!-- G3 (do NOT repeat the 4-step migration PROCEDURE). S2 owns it in
     qmk-integration.md. Show ONE worked before/after RECIPE + link out for the
     full procedure. -->

<!-- G4 (matcher parity vocabulary). The host matcher is a FULL-PARITY port of the
     firmware pattern_match.c (* ^ $ WT + and \d \D \w \W \s \S \b \B .).
     Say so in Example 4 so readers know their firmware patterns translate. A
     single-string match = class-only (when the window has a title); a 2-element
     array ["class","title"] = class+title (== WT()). -->

<!-- G5 (host layers are >= 224). Recipes must use layer numbers >= 224 (255 = clear).
     Do NOT copy the firmware examples' low layers (0/1/2/3) into rules.toml —
     translate them UP (firmware _GAMING = 1 -> host layer = 224). Call the
     translation out explicitly. -->

<!-- G6 (the stack/replace decision is per-WINDOW, an AND over matched rules). A
     single non-disabling matched rule makes the whole window STACK. Don't imply
     each rule independently stacks/replaces. The Example 4 runtime walkthrough
     must state this. -->

<!-- G7 (DEFINE_HOST_CALLBACKS row shape). { name, on_enable, on_disable };
     on_disable may be NULL; id = array index. The pattern is NOT a field of the
     registry (the pattern moves to rules.toml). Match the canonical `mute` form
     from the firmware README. -->

<!-- G8 (Jekyll Markdown). Use LABELED fences (```c / ```toml / ```bash — rouge
     highlights them; never bare ``` for code). Internal links:
     {{ site.baseurl }}/slug (NOT https, NOT .md). The page permalinks are
     /examples/ and /troubleshooting/ (in front matter — don't touch them). -->
```

## Implementation Blueprint

### Document structure (the deliverables)

The new content. Exact wording is in the Implementation Tasks; this is the shape.
Reuse Example 2's callback/app names for page continuity (research §0.3); do NOT
copy the §9 alacritty/neovide reference (research §2/G2).

#### A. The new `## Example 4` section in `docs/examples.md`

```markdown
## Example 4: Host-Side Rules (`rules.toml`)

**Scenario**: You want the same app→layer / app→callback behavior as Examples 1–3,
but editable from your desktop **without reflashing**. Host rules live in an
optional `rules.toml` file; QMKonnect does the matching on the host and pushes
layer/callback decisions to the keyboard over Raw HID.

> Host rules **stack on top of** your board's `DEFINE_SERIAL_*` rules (the board's
> rules run first, then host rules apply on top) — unless a rule opts into
> "replace" mode. They require firmware that advertises the typed-command
> capability (`proto_ver == 2`); legacy firmware silently falls back to today's
> string-only behavior. The host matcher is a **full-parity port** of the firmware
> `pattern_match.c`, so the patterns from Examples 1–3 (`*`, `WT(class,title)`,
> `^`, `$`, `+`, `\d`, …) translate directly. See the
> [Configuration Guide]({{ site.baseurl }}/configuration) for the complete schema,
> and the [QMK Integration Guide]({{ site.baseurl }}/qmk-integration) for the
> one-time firmware change and the full migration procedure.

### Before: firmware rules (Example 2, baked into your keymap)

This is the gaming/office setup from Example 2, expressed as firmware rules. To
enable host rules you add a **one-time** named callback registry and keep your
existing `DEFINE_SERIAL_*` rules:

```c
// One-time firmware change: name the callbacks you already use.
static void enable_gaming_mode(void)  { /* disable Win key, Alt-Tab, … */ }
static void disable_gaming_mode(void) { /* re-enable all keys */ }

DEFINE_HOST_CALLBACKS({
    { "enable_gaming",  &enable_gaming_mode,  &disable_gaming_mode },
    { "disable_gaming", &disable_gaming_mode, &enable_gaming_mode  },  // on_disable may be NULL
});

DEFINE_SERIAL_LAYERS({
    { "steam_app*", _GAMING }, { WT("cs2", "*"), _GAMING }, { "*word*", _OFFICE },
});
DEFINE_SERIAL_COMMANDS({
    { "steam_app*", &enable_gaming_mode }, { "*word*", &disable_gaming_mode },
});
```

### After: the same rules as `rules.toml`

Move those rules into `rules.toml` (alongside `config.toml`). Host layers are
reserved **≥ 224** (so they resolve above your board layers), so the firmware
`_GAMING = 1` becomes host `layer = 224`:

```toml
[host]
disable_firmware_config = false   # global default: STACK (board rules still run)

# Layer rules — FIRST match wins. Host layers are >= 224.
[[layer_rules]]
match = "steam_app*"                       # class-only pattern (board rules also run)
layer = 224

[[layer_rules]]
match = ["cs2", "Counter-Strike 2"]        # [class, title]  == WT(class, title)
layer = 224

# Replace: for this window the host takes over and the board is skipped.
[[layer_rules]]
match = ["*chrome*", "*youtube*"]
layer = 225
disable_firmware_config = true

# Callback rules — ALL matches fire. Names come from DEFINE_HOST_CALLBACKS.
[[callback_rules]]
match = "steam_app*"
enable = ["enable_gaming"]

[[callback_rules]]
match = "*word*"
enable = ["disable_gaming"]
```

After moving a rule, **remove it from `DEFINE_SERIAL_LAYERS` /
`DEFINE_SERIAL_COMMANDS`** (keeping a layer rule in both is harmless but
confusing; keeping a callback rule in both fires it twice). See the
[QMK Integration Guide]({{ site.baseurl }}/qmk-integration) for the full 4-step
procedure, and run `qmkonnect --validate-rules` after editing.

### What happens at runtime

The stack-vs-replace decision is made **per window** (a window is *replace* only
when **every** rule that matches it is disabling — otherwise it *stacks*):

- **A Steam game** (`steam_app_*`) → **stack**. The board runs its own
  `DEFINE_SERIAL_*` rules for that window, the host layer `224` applies **on top**,
  and the host `enable_gaming` callback fires after the board callbacks.
- **YouTube in Chrome** (`*chrome*` + `*youtube*`) → **replace** (`disable_firmware_config = true`).
  No window string is sent, so the board can't match; the firmware clears its own
  board layer/command and applies only the host layer `225`.
- **MS Word** (`*word*`) → only a callback rule matches (no layer rule), so the
  host layer is unchanged and the board runs normally; the host `disable_gaming`
  callback fires. If **no** rule matches a window, the host layer is cleared and
  all host callbacks are disabled.

Run `qmkonnect --list-callbacks` to see your keyboard's real callback names, and
see the [Configuration Guide]({{ site.baseurl }}/configuration) for every field.
```

> *(The `[[layer_rules]]` / `[[callback_rules]]` / `match` / `layer` /
> `disable_firmware_config` keys are documented field-by-field in configuration.md
> (S1) — link out, don't re-document them here. The 4-step migration procedure is
> in qmk-integration.md (S2) — link out, don't repeat it.)*

#### B. The new `## Host Rules Issues` section in `docs/troubleshooting.md`

Four entries, each `**Symptoms**` / numbered `**Solutions**` (or Cause/Diagnose/Fix)
/ ```bash blocks. The diagnostic commands cite the **shipped** CLI output verbatim
(research §0.4). Exact wording is in the Implementation Tasks (Task 3); the shape
per entry:

```markdown
## Host Rules Issues

Host-side rules (`rules.toml`) have a few distinct failure modes. If your board's
firmware rules work but host rules don't, check these first. See the
[Configuration Guide]({{ site.baseurl }}/configuration) for the schema and CLI flags.

### Legacy firmware (`proto_ver != 2`) — host rules disabled

**Symptoms**: `rules.toml` is present and valid, but host rules have no effect;
the keyboard still switches layers via its firmware rules.

**Cause**: host rules require firmware that advertises the typed-command
capability (`proto_ver == 2`). With legacy firmware (or while the keyboard is
disconnected) QMKonnect silently falls back to string-only mode and never sends
host commands — your board's `DEFINE_*` rules keep working unchanged.

**Diagnose**:
```bash
qmkonnect --list-callbacks
# Legacy firmware prints exactly:
#   Legacy firmware (no callback support) — host rules will run in string-only mode.
# (exit 0 — this is expected, not an error)
```

**Fix**: flash firmware that defines `DEFINE_HOST_CALLBACKS` (which advertises
`proto_ver == 2`). See the [QMK Integration Guide]({{ site.baseurl }}/qmk-integration).
Until then, nothing is broken — your firmware rules work as before.

### Callback name not found in registry

**Symptoms**: a host callback doesn't fire, or `--validate-rules` warns about an
unknown callback name.

**Cause**: `rules.toml` references a callback **name** that isn't in your
`DEFINE_HOST_CALLBACKS` registry — a typo, a name you haven't registered yet, or
firmware that hasn't been reflashed with the new registry.

**Diagnose**:
```bash
qmkonnect --validate-rules        # unknown names print:  ⚠  unknown callback: {name}
                                  # (a WARNING — exit 0, NOT fatal)
qmkonnect --list-callbacks        # prints the keyboard's real callback name -> id table
```

**Fix**: correct the name in `rules.toml` to match a name printed by
`--list-callbacks`, **or** add it to `DEFINE_HOST_CALLBACKS` and reflash. Names
are case-sensitive strings. See the
[Configuration Guide]({{ site.baseurl }}/configuration) (`--validate-rules` /
`--list-callbacks`) and the
[QMK Integration Guide]({{ site.baseurl }}/qmk-integration) (`DEFINE_HOST_CALLBACKS`).

### `rules.toml` parse error

**Symptoms**: `--validate-rules` exits **non-zero** and prints `rules.toml
invalid: …`; host rules are disabled (string-only fallback) until the file parses.

**Cause**: malformed TOML, a missing required key, or a bad `match` form.

**Diagnose**:
```bash
qmkonnect --validate-rules                  # prints:  rules.toml invalid: {error}  (exit non-zero)
qmkonnect --validate-rules --rules-path ~/rules.draft.toml   # validate a draft elsewhere
```

**Fix**: every `[[layer_rules]]` entry **requires** `match` and `layer` (an entry
missing either is an error); `match` is either a bare string (`"steam_app*"`,
class-only) or a **2-element** array (`["*chrome*", "*youtube*"]` — class and
title; 1- or 3-element arrays are errors); `layer` must be **≥ 224**. See the
[Configuration Guide]({{ site.baseurl }}/configuration) for the full field table.

### Device shows connected but rules not applying

**Symptoms**: the keyboard is connected and firmware rules work, but your
`rules.toml` rules have no effect.

**Checklist**:
1. **`rules.toml` present and valid?** `qmkonnect --validate-rules` (a missing
   file prints `No rules.toml found (host rules disabled). Nothing to validate.`
   and exits 0 — host rules are simply off; create it with `qmkonnect -c` or
   hand-write it next to `config.toml`).
2. **Firmware capable?** `qmkonnect --list-callbacks` — the "Legacy firmware …"
   line means host rules are off (see *Legacy firmware* above).
3. **Pattern matches the real window class?** The matcher is class-only for a
   bare `match` string. Check what QMKonnect actually sees:
   ```bash
   qmkonnect -v | grep -i "window\|sending"     # the class\x1Dtitle string sent
   ```
   (or use the tray's "Show Window Information"). A `*chrome*` rule won't match a
   class reported as `Google Chrome` — adjust the pattern or use a `[class, title]`
   array.
4. **Reloaded after editing?** Click the tray's **Reload rules** item (macOS,
   Windows, and Linux all have it) or restart QMKonnect — `rules.toml` is not
   re-read on every focus change.
5. **Callback name correct?** `qmkonnect --list-callbacks` (see *Callback name
   not found* above).
6. **Layer ≥ 224?** Host layers must be ≥ 224 (`255` clears); a low number won't
   resolve above your board layers.

See the [Configuration Guide]({{ site.baseurl }}/configuration) for the schema and
CLI flags, and the [Examples]({{ site.baseurl }}/examples) for a complete recipe.
```

### Implementation Tasks (ordered by dependencies)

```yaml
Task 1: READ both files + research/notes.md + the spec sources + the shipped CLI code
  - READ docs/examples.md in full (8.0 KB) to confirm the skeleton + the Example 2
    callback/app names (enable_gaming_mode/disable_gaming_mode; steam_app*, cs2,
    *word*, *excel*) that Example 4 reuses, and the insertion boundary: after
    Example 3's "**Result**:" line (L229) / before "## Pattern Matching Tips" (L231).
  - READ docs/troubleshooting.md in full to confirm the skeleton + the existing
    "**Symptoms**/**Solutions**" entry format, and the insertion boundary: between
    the end of "## Configuration Issues" (~L455) and "## Performance Issues" (L456).
  - READ plan/002_637d65b6e9b8/P6M1T1S3/research/notes.md §0.2 (spec sources),
    §0.3 (callback names), §0.4 (the SHIPPED CLI messages + exit codes), §0.5 (tray
    Reload rules on all platforms), §1 (skeletons).
  - READ spec/HOST_RULES.md §4 (stack/replace) + §8(2) (matcher) + §8(6) (CLI) to
    confirm the runtime model + the matcher vocabulary.
  - READ src/main.rs fn list_callbacks + fn validate_rules (≈L261-355) to confirm
    the EXACT shipped strings + exit codes to quote in the FAQ.
  - READ /home/dustin/projects/qmk-notifier/README.md ("Host-Side Rules & Typed
    Commands") to confirm the DEFINE_HOST_CALLBACKS `mute` form + the stack/replace
    prose.
  - GOTCHA G1: the feature is shipped (P1-P5 Complete). No code-blocker hedges.

Task 2: INSERT the new "## Example 4: Host-Side Rules (rules.toml)" section
        into docs/examples.md (blueprint block A)
  - INSERT the entire new section (scenario blockquote + firmware "before" ```c
    block + rules.toml "after" ```toml block + "What happens at runtime" walkthrough
    + before/after note) AFTER Example 3's "**Result**:" line and BEFORE
    "## Pattern Matching Tips".
  - WRITE the firmware "before" ```c block reusing Example 2's names: a one-time
    DEFINE_HOST_CALLBACKS({ { "enable_gaming", &enable_gaming_mode, &disable_gaming_mode },
    { "disable_gaming", &disable_gaming_mode, &enable_gaming_mode } }) + the
    DEFINE_SERIAL_LAYERS / DEFINE_SERIAL_COMMANDS rows being migrated. Note
    on_disable may be NULL and the pattern is NOT a registry field.
  - WRITE the rules.toml "after" ```toml block as a DIFFERENT scenario from §9
    (gaming/office — NO alacritty/neovide). Include: [host] disable_firmware_config
    = false (stack default); a class-only match ("steam_app*"); a class+title match
    (["cs2", "Counter-Strike 2"]); a [[callback_rules]] enable; and at least one
    disable_firmware_config = true (replace) rule. Use host layers >= 224 (NOT the
    firmware 0/1/2/3) — call the translation out.
  - WRITE the "What happens at runtime" paragraph walking >=2 windows (a STACK
    window: board rules run + host layer on top; a REPLACE window: board skipped via
    clear_board; optionally a callback-only / no-match window), stating the
    per-WINDOW AND decision (one non-disabling matched rule => stack).
  - ADD the before/after note ("remove the migrated rows from DEFINE_*; link to
    /qmk-integration for the full 4-step procedure; run --validate-rules").
  - ADD cross-links via {{ site.baseurl }}: /configuration (schema + CLI flags) and
    /qmk-integration (migration). Optionally /troubleshooting.
  - (OPTIONAL) append ONE line to the L9 intro blockquote flagging Example 4 as the
    host-side alternative (the existing "only sends the active-window string" claim
    is accurate for Examples 1-3 but not for host rules).
  - KEEP Examples 1-3, Pattern Matching Tips, Testing, Next Steps VERBATIM (insert only).
  - GOTCHA G2: NO "alacritty" anywhere in examples.md (the §9 marker — S1 owns it).
  - GOTCHA G5: host layers >= 224 in the recipe.
  - GOTCHA G6: the per-window AND decision in the runtime walkthrough.
  - GOTCHA G7: DEFINE_HOST_CALLBACKS row = { name, on_enable, on_disable }.
  - VERIFY: grep -c 'rules.toml' (>=5); grep -c 'alacritty' (==0);
    grep 'disable_firmware_config' (both true and false); grep 'site.baseurl.*/configuration'.

Task 3: INSERT the new "## Host Rules Issues" section into docs/troubleshooting.md
        (blueprint block B)
  - INSERT the new H2 section (intro line + 4 ### entries) between the end of
    "## Configuration Issues" (~L455) and "## Performance Issues" (L456).
  - WRITE the 4 entries, each matching the CONTRACT names: (1) "Legacy firmware
    (proto_ver != 2) — host rules disabled"; (2) "Callback name not found in
    registry"; (3) "rules.toml parse error"; (4) "Device shows connected but rules
    not applying". Each in the file's existing **Symptoms** / numbered **Solutions**
    (or Cause/Diagnose/Fix) / ```bash-block format.
  - CITE the SHIPPED CLI behavior VERBATIM (research §0.4, from src/main.rs):
    --list-callbacks legacy line "Legacy firmware (no callback support) — host rules
    will run in string-only mode." (exit 0); --validate-rules "rules.toml invalid:
    {e}" (non-zero) on parse/schema error; "⚠  unknown callback: {name}" (warning,
    exit 0) on unknown names; "No rules.toml found (host rules disabled). Nothing to
    validate." (exit 0) when no file. Do NOT paraphrase — users grep for these.
  - CITE schema facts inline with links: required match + layer; match is a string
    or 2-element array; layer >= 224. (Do NOT paste the field table — link to
    /configuration.)
  - For "Reload rules" say "Click the tray's **Reload rules** item (macOS, Windows,
    and Linux all have it)" — NO platform hedge (P5.M2 is Complete on all three).
  - ADD cross-links via {{ site.baseurl }}: /configuration (schema + CLI) and
    /qmk-integration (DEFINE_HOST_CALLBACKS + migration); the last entry also links
    /examples.
  - KEEP all existing sections VERBATIM (insert only).
  - GOTCHA G8: labeled ```bash fences; {{ site.baseurl }}/slug links.
  - VERIFY: grep -n 'Host Rules Issues\|Legacy firmware\|Callback name not found\|
    parse error\|rules not applying' (all 4 entries); grep -n 'list-callbacks\|
    validate-rules' (the diagnostic commands); grep 'site.baseurl.*/configuration'.

Task 4: VALIDATE (Markdown — NO cargo)
  - RUN the §5 validation commands from research/notes.md:
      git diff --stat                 # expect examples.md + troubleshooting.md ONLY
      grep -c 'rules.toml' docs/examples.md                          # >= 5
      grep -c 'alacritty' docs/examples.md                           # == 0 (no S1 duplication)
      grep -n 'disable_firmware_config' docs/examples.md             # both true and false
      grep -n 'Host Rules Issues' docs/troubleshooting.md            # the new section
      grep -n 'Legacy firmware\|unknown callback\|rules.toml invalid\|rules not applying' docs/troubleshooting.md
      test $(( $(grep -c '^```' docs/examples.md) % 2 )) -eq 0 && echo OK || echo UNBALANCED
      test $(( $(grep -c '^```' docs/troubleshooting.md) % 2 )) -eq 0 && echo OK || echo UNBALANCED
      grep -oE '\{\{ site\.baseurl \}\}/[a-z-]+' docs/examples.md | sort -u
      grep -oE '\{\{ site\.baseurl \}\}/[a-z-]+' docs/troubleshooting.md | sort -u
  - (Optional) Jekyll build if ruby/bundle present: cd docs && bundle exec jekyll build.
  - EYEBALL: Example 4's runtime walkthrough is correct (per-window AND); the 4 FAQ
    entries cite the shipped CLI messages verbatim; no sibling content duplicated.
```

### Implementation Patterns & Key Details

```markdown
<!-- PATTERN: examples.md uses "**Scenario**:" + "### QMK Keymap Configuration" + a ```c
     block + "**Result**:" per example. Example 4 mirrors this but the "configuration"
     is rules.toml, so use sub-headings "### Before: firmware rules" / "### After: the
     same rules as rules.toml" / "### What happens at runtime" with ```c + ```toml fences. -->

<!-- PATTERN: troubleshooting.md uses "### <Entry>" + "**Symptoms**: ..." + "**Solutions**:"
     (or "**Cause**"/"**Diagnose**"/"**Fix**") + numbered lists + ```bash blocks. Match
     that exactly for the 4 new entries (see "### Keyboard Not Detected",
     "### Invalid Configuration File" for the house style). -->

<!-- PATTERN: internal links are `[text]({{ site.baseurl }}/slug)` — every existing link
     in both files uses this. The slugs: /configuration (S1), /qmk-integration (S2),
     /examples, /troubleshooting, /installation. NO .md, NO absolute URLs for internal. -->

<!-- KEY DETAIL: Example 4 reuses Example 2's callback/app names (enable_gaming_mode /
     disable_gaming_mode / steam_app* / cs2 / *word*) for page continuity — the reader
     just saw them. This is INTENTIONALLY distinct from the §9 alacritty/neovide
     reference (which S1 embeds verbatim in configuration.md). -->

<!-- KEY DETAIL: cite schema facts INLINE with a link rather than reproducing the table.
     E.g. "every [[layer_rules]] entry requires match and layer; layer must be >= 224;
     match is a string or 2-element array — see the Configuration Guide for the full
     table." This avoids duplicating S1 while staying actionable. -->

<!-- KEY DETAIL: the CLI messages in the FAQ are the SHIPPED strings from src/main.rs
     (research §0.4). Quote them exactly: --list-callbacks "Legacy firmware (no callback
     support) — host rules will run in string-only mode."; --validate-rules "rules.toml
     invalid: {e}" and "⚠  unknown callback: {name}" and "No rules.toml found (host
     rules disabled). Nothing to validate.". Do NOT paraphrase — users grep for them. -->
```

### Integration Points

```yaml
DOCUMENT STRUCTURE (examples.md): the new "## Example 4" is INSERTED after Example 3
  and before "## Pattern Matching Tips". No existing example is moved/removed; the
  only optional existing-content touch is one appended line in the L9 intro blockquote.

DOCUMENT STRUCTURE (troubleshooting.md): the new "## Host Rules Issues" H2 is INSERTED
  between "## Configuration Issues" and "## Performance Issues". No existing section is
  moved/removed.

CROSS-LINKS (added — Jekyll {{ site.baseurl }}):
  examples.md → /configuration (schema + CLI flags — S1); /qmk-integration (migration — S2);
                optionally /troubleshooting.
  troubleshooting.md → /configuration (schema + CLI — S1); /qmk-integration (firmware/
                       migration — S2); /examples (recipe — this task).

INBOUND LINKS (from siblings): configuration.md (S1) and qmk-integration.md (S2) both
  link to {{ site.baseurl }}/examples and {{ site.baseurl }}/troubleshooting. The new
  Example 4 + Host Rules Issues sections are the targets — they must contain real
  recipes/FAQs so those links resolve to substantive content.

DOWNSTREAM CONSUMERS:
  - P6.M1.T1.S4 (README.md + llms_full.txt regen): llms_full.txt is a GENERATED
    concatenation of docs/*.md. This task's two file edits will be picked up when S4
    regenerates it AFTER all doc edits land. Do NOT regenerate it here.

BUILD: none. Jekyll (docs/_config.yml: markdown kramdown, highlighter rouge,
  baseurl "/qmkonnect") renders both pages on GitHub Pages. An optional local
  `bundle exec jekyll build` validates Liquid/fence syntax but is not required.
```

## Validation Loop

### Level 1: Markdown consistency (Immediate Feedback)

```bash
cd /home/dustin/projects/qmkonnect

# Scope gate: ONLY the two files changed.
git diff --stat
# EXPECT: docs/examples.md AND docs/troubleshooting.md ONLY.

# examples.md: the recipe + before/after landed.
grep -c 'rules.toml' docs/examples.md                      # >= 5
grep -n 'Example 4\|Host-Side Rules' docs/examples.md      # the new section
grep -n 'disable_firmware_config' docs/examples.md         # the stack/replace flag (true AND false)
grep -ni 'stack\|replace' docs/examples.md                 # the runtime walkthrough
grep -n 'DEFINE_HOST_CALLBACKS' docs/examples.md           # the firmware "before" block

# troubleshooting.md: the 4 entries landed.
grep -n 'Host Rules Issues' docs/troubleshooting.md
grep -n 'Legacy firmware\|Callback name not found\|parse error\|rules not applying' docs/troubleshooting.md
grep -n -- '--list-callbacks\|--validate-rules' docs/troubleshooting.md   # diagnostic commands

# Balanced code fences in BOTH files (even count of ``` lines).
test $(( $(grep -c '^```' docs/examples.md) % 2 )) -eq 0 \
  && echo "examples fences balanced" || echo "examples UNBALANCED — fix before committing"
test $(( $(grep -c '^```' docs/troubleshooting.md) % 2 )) -eq 0 \
  && echo "troubleshooting fences balanced" || echo "troubleshooting UNBALANCED — fix before committing"

# Internal link slugs are well-formed (no .md, no https for internal).
grep -oE '\{\{ site\.baseurl \}\}/[a-z-]+' docs/examples.md | sort -u
grep -oE '\{\{ site\.baseurl \}\}/[a-z-]+' docs/troubleshooting.md | sort -u
```

### Level 2: Content-fidelity cross-check (Component Validation)

```bash
cd /home/dustin/projects/qmkonnect

# Example 4 is a DIFFERENT scenario from the §9 reference (no alacritty/neovide).
grep -c 'alacritty' docs/examples.md          # EXPECT 0 (S1 owns the §9 example)
grep -c 'neovide' docs/examples.md            # EXPECT 0

# Example 4 reuses Example 2's callback/app names for continuity.
grep -n 'enable_gaming\|disable_gaming\|steam_app\|cs2' docs/examples.md   # the recipe

# Example 4 uses host layers >= 224 (NOT firmware 0/1/2/3).
grep -n 'layer = 22\|layer = 23\|>= 224\|≥ 224' docs/examples.md

# Example 4 demonstrates BOTH stack and replace + the per-window AND decision.
grep -ni 'disable_firmware_config = true\|disable_firmware_config = false' docs/examples.md
grep -ni 'every.*match\|one non-disabling\|per window' docs/examples.md

# The 4 troubleshooting entries cite the SHIPPED CLI messages (research §0.4) verbatim.
grep -n 'string-only mode' docs/troubleshooting.md                 # --list-callbacks legacy line
grep -n 'rules.toml invalid' docs/troubleshooting.md               # --validate-rules parse error
grep -n 'unknown callback' docs/troubleshooting.md                 # --validate-rules warning
grep -n 'Nothing to validate' docs/troubleshooting.md              # no rules.toml info
grep -n 'class-only\|2-element\|two-element' docs/troubleshooting.md # match form facts

# The "Reload rules" advice has NO platform hedge (ships on all three).
grep -ni 'macOS, Windows, and Linux\|all.*platforms' docs/troubleshooting.md
! grep -qi 'where available' docs/troubleshooting.md && echo "no platform hedge (good)" || echo "STALE HEDGE — remove"
```

### Level 3: Render check (System Validation — optional, highest confidence)

```bash
cd /home/dustin/projects/qmkonnect/docs

# If ruby + bundler are available, build the Jekyll site. Catches Liquid syntax
# errors, broken {{ site.baseurl }} expansions, and malformed fences that a grep
# can miss. (Not required for correctness — GitHub Pages renders on push — but
# it's the strongest local signal.)
bundle exec jekyll build 2>&1 | grep -iE 'error|warning' || echo "jekyll build clean"

# Then open _site/examples/index.html and _site/troubleshooting/index.html and confirm:
#   - examples.md: "Example 4: Host-Side Rules" appears after Example 3; the ```c
#     "before" and ```toml "after" blocks are syntax-highlighted (rouge); the
#     cross-links to /configuration/ and /qmk-integration/ resolve.
#   - troubleshooting.md: "Host Rules Issues" appears between "Configuration Issues"
#     and "Performance Issues"; the 4 ### entries render with their ```bash blocks;
#     the cross-links resolve.
#   - all existing sections in both files are unchanged.
```

### Level 4: Boundary + scope gate (Domain-Specific)

```bash
cd /home/dustin/projects/qmkonnect

# Boundary: this task did NOT duplicate the sibling tasks' content.
# S1 owns the §9 reference example + the field table + the CLI flag TABLE.
grep -c 'alacritty' docs/examples.md            # 0 — the §9 marker is absent
# (Citing --list-callbacks / --validate-rules by NAME in the FAQ is REQUIRED and is
#  not duplication — S1 owns the flag REFERENCE TABLE; S3 cites them in context with
#  the shipped output.)
git diff --name-only                             # ONLY examples.md + troubleshooting.md

# The "before" firmware block in Example 4 uses DEFINE_SERIAL_* (fine — it's the
# migration "before" state, not a duplication of S2's procedure).
grep -c 'DEFINE_SERIAL_LAYERS\|DEFINE_SERIAL_COMMANDS' docs/examples.md   # >= 1 (the before block)
# The 4-step migration PROCEDURE is NOT pasted (S2 owns it) — only a link.
grep -c 'Move a layer rule\|Expose.*callbacks by name' docs/examples.md    # 0 (procedure words absent)
```

## Final Validation Checklist

### Technical Validation
- [ ] `git diff --stat` = `docs/examples.md` + `docs/troubleshooting.md` ONLY (scope gate).
- [ ] ```c / ```toml / ```bash fences balanced in BOTH files (Level 1).
- [ ] All `{{ site.baseurl }}/slug` cross-links well-formed; slugs include
      `/configuration` and `/qmk-integration` (Level 1).
- [ ] (If run) `bundle exec jekyll build` reports no Liquid/fence errors (Level 3).

### Feature Validation (contract fidelity — item CONTRACT points 1–5)
- [ ] **examples.md**: new `## Example 4: Host-Side Rules (rules.toml)` inserted
      after Example 3, before `## Pattern Matching Tips`. [CONTRACT 1,3]
- [ ] **examples.md**: a **complete `rules.toml` example** showing layer rules
      (class-only + class+title), callback rules, and `disable_firmware_config`
      (both the `[host]` default and an explicit `= true`). [CONTRACT 3]
- [ ] **examples.md**: a **before/after migration** from `DEFINE_*` — the firmware
      `DEFINE_HOST_CALLBACKS` + `DEFINE_SERIAL_*` "before" → the `rules.toml`
      "after" + a mapping note. [CONTRACT 3]
- [ ] **examples.md**: the **stack/replace model** shown concretely in a runtime
      walkthrough (a STACK window + a REPLACE window; per-window AND decision).
      [CONTRACT 3]
- [ ] **troubleshooting.md**: new `## Host Rules Issues` section with the FOUR
      entries: *Legacy firmware (proto_ver != 2) — host rules disabled*; *Callback
      name not found in registry*; *rules.toml parse error*; *Device shows
      connected but rules not applying*. [CONTRACT 3]
- [ ] Each troubleshooting entry cites the **shipped CLI behavior verbatim**
      (legacy line; `rules.toml invalid` non-zero; `⚠  unknown callback` warning
      exit 0; `Nothing to validate` info). [CONTRACT 2]
- [ ] Existing sections in both files unchanged (insert-only; optional intro line).
      [scope]

### Code Quality Validation (documentation)
- [ ] Example 4's recipe is a DIFFERENT scenario from §9 (no `alacritty`/`neovide`).
- [ ] Example 4 uses host layers ≥ 224 (firmware low layers translated up).
- [ ] The runtime walkthrough's per-window AND decision is stated correctly.
- [ ] `DEFINE_HOST_CALLBACKS` "before" uses the canonical `{ name, on_enable,
      on_disable }` row shape (matches the firmware README `mute` form).
- [ ] Markdown fence / link / list formatting consistent with each file's house style.
- [ ] No duplication of S1 (schema/§9/CLI table) / S2 (4-step procedure) /
      S4 (llms_full.txt) — only cross-links out.

### Documentation & Deployment
- [ ] Plain-language explanations (esp. the runtime walkthrough, the FAQ fixes) for
      a non-spec-reader audience.
- [ ] Cross-links point at the right sibling pages (configuration for schema/CLI;
      qmk-integration for firmware/migration).
- [ ] No platform hedge on the tray "Reload rules" item (it ships on all three).

---

## Anti-Patterns to Avoid

- ❌ Don't **paste the §9 reference example or the field table** into examples.md.
      S1 owns those verbatim in configuration.md. Example 4's recipe must be a
      DIFFERENT scenario (the gaming/office setup reusing Example 2's names); the
      §9 marker `alacritty` must be ABSENT from examples.md. Cite schema facts
      inline with a link to `/configuration` (G2).
- ❌ Don't **repeat the 4-step migration procedure**. S2 owns it in
      qmk-integration.md. Show ONE worked before/after recipe + link out (G3).
- ❌ Don't **reuse the firmware examples' low layer numbers** (0/1/2/3) in
      `rules.toml`. Host layers are ≥ 224 (`255` clears); translate the firmware
      layer UP and call out the translation (G5).
- ❌ Don't **describe the stack/replace decision as per-rule**. It's per-WINDOW (an
      AND over all matched rules); one non-disabling matched rule makes the whole
      window "stack" (G6).
- ❌ Don't **invent a `DEFINE_HOST_CALLBACKS` struct**. Copy the canonical row shape
      `{ name, on_enable, on_disable }` (`on_disable` may be `NULL`; `id` = array
      index; pattern is NOT a registry field) from the firmware README (G7).
- ❌ Don't **paraphrase the shipped CLI messages/exit codes** in the FAQ. Quote them
      verbatim from `src/main.rs` (research §0.4) — users grep for them (e.g.
      `"Legacy firmware (no callback support) — host rules will run in string-only
      mode."`, `"rules.toml invalid: {e}"`, `"⚠  unknown callback: {name}"`).
- ❌ Don't **hedge the tray "Reload rules" item**. P5.M2 is Complete on all three
      platforms — say "click the tray's Reload rules item" with no "(where
      available)" qualifier.
- ❌ Don't **regenerate `docs/llms_full.txt`**. It's a generated concatenation owned
      by P6.M1.T1.S4, regenerated after all doc edits land.
- ❌ Don't **edit the existing sections** in either file beyond the optional
      examples.md intro line. The new content is INSERTED, not folded into existing
      prose.
- ❌ Don't use **absolute URLs or `.md` extensions** for internal links. Use
      `{{ site.baseurl }}/slug` to match every existing link in both files (G8).
- ❌ Don't use **unlabeled code fences**. Use ```c / ```toml / ```bash (rouge
      highlights them), matching each file's existing style (G8).
- ❌ Don't **edit any file other than `docs/examples.md` and
      `docs/troubleshooting.md`** — no Rust, no Cargo, no other docs, no spec, no
      plan files (except this item's own PRP/research).

---

## Confidence Score: 9/10

This is a **well-bounded, two-file documentation task** with authoritative,
locally available sources — and now, critically, the **feature is shipped** so the
FAQ cites **real CLI behavior read from `src/main.rs`** (`fn list_callbacks` /
`fn validate_rules`, research §0.4), not a spec prediction. Other sources: the
locked **spec** (`spec/HOST_RULES.md` §4 stack/replace model, §8(2) matcher
parity, §9 the reference-to-NOT-duplicate, §10 migration), the **canonical
firmware README** (`DEFINE_HOST_CALLBACKS` `mute` form + stack/replace prose), and
the **line-accurate skeletons + insertion points of both target files** (research
§1). The boundary with S1 (schema/§9/CLI-table — link out), S2 (4-step procedure —
link out), and S4 (`llms_full.txt` regen) is explicit, and the recipe is
deliberately a **different scenario** from §9 (reuses Example 2's gaming/office
names) so it cannot duplicate S1. The tray "Reload rules" item is confirmed on all
three platforms (no hedge). Scope is cleanly two files; the anti-patterns guard
against the three most likely slips (duplicating the §9 example, reusing low
firmware layer numbers in `rules.toml, and paraphrasing the shipped CLI messages).
The 1-point reservation is the **optional examples.md intro-blockquote touch** —
appending one flagging line is the honest fix, but it's optional and a no-op if
skipped.