# PRP — P6.M1.T1.S1: Update `docs/configuration.md` with `rules.toml` schema + CLI flags

> **Repo under change:** the **qmkonnect** desktop app at
> `/home/dustin/projects/qmkonnect`. This is a **documentation-only** task (item
> CONTRACT: "DOCS: This IS the documentation subtask"). It edits **ONE** Markdown
> file — `docs/configuration.md` (341 lines / 10.8 KB, UNCHANGED since the
> host-rules feature landed) — to add: the `rules.toml` file location, the full
> schema reference (incl. the complete annotated `HOST_RULES.md` §9 example), the
> `disable_firmware_config` stack/replace semantics, the three new CLI flags
> (`--list-callbacks`, `--validate-rules`, `--rules-path`), and the note that
> `-c`/`--config` now also seeds a commented `rules.toml` template.
>
> **File touched (1):** `docs/configuration.md`. **Nothing else** — no Rust, no
> Cargo, no other docs, no `llms_full.txt` (regenerated by P6.M1.T1.S4).
>
> **STATUS: all code the docs describe is LANDED and the build PASSES.** P3.M1
> (the `rules.toml` model), P4.M2.T1.S1 (the handshake), and **P5.M1.T1.S1 (the
> CLI flags)** are all merged; `cargo build --bin qmkonnect` ⇒ EXIT 0. Every CLI
> output string / exit code in this PRP is **verified against the actual landed
> `src/main.rs`** (line numbers cited), not a spec contract. The implementer can
> — and SHOULD — run `cargo run -- --help` / `--validate-rules` / `-c` to confirm
> the docs match reality (Validation Level 3).

---

## Goal

**Feature Goal**: Make `docs/configuration.md` the complete, accurate
**user-facing reference** for the host-side-rules feature. After reading the new
section, a user who has never seen `rules.toml` knows: (a) where the file lives
on their OS, (b) every field/section/key and which are required, (c) how
`disable_firmware_config` decides stack-vs-replace per window, (d) how to create
the file (`qmkonnect -c` seeds a commented template), and (e) the three new CLI
flags and their exit-code semantics — with the canonical annotated `HOST_RULES.md`
§9 example to copy/adapt. After the CLI flags subsection, a reader knows exactly
what `--list-callbacks` / `--validate-rules` / `--rules-path` print and when they
exit non-zero.

**Deliverable** (`docs/configuration.md`, edited):
1. A new top-level section **`## Host Window Rules (rules.toml)`** with three
   subsections — `### File location`, `### Schema reference` (field table + the
   verbatim §9 annotated example + a "creating it" note), and
   `### Stack vs. replace (disable_firmware_config)`.
2. The existing **`### CLI flags`** table extended with three new rows
   (`--list-callbacks`, `--validate-rules`, `--rules-path`) and the `-c`/`--config`
   row annotated to note it now also seeds `rules.toml`.
3. A short `-c` seeding note in the new "Schema reference → creating it"
   subsection (cross-referenced from the CLI table).

**Success Definition**:
- The new section renders cleanly under Jekyll (kramdown + rouge; no broken
  ```toml fences, no malformed Liquid `{{ site.baseurl }}` links).
- The schema field table matches `src/core/rules.rs` **exactly** (field names,
  TOML keys, required-vs-optional, `bool`-vs-`Option<bool>` for
  `disable_firmware_config`, the ≥ 224 layer range).
- The annotated example is the **verbatim** `HOST_RULES.md` §9 block.
- The `disable_firmware_config` explanation correctly states: global default in
  `[host]`, optional per-rule override (absent ⇒ inherits `[host]`), the window
  is "replace" iff **every** matched rule's effective flag is `true` (or the board
  has no rules), no-match always clears, and absence of `rules.toml` ⇒ feature
  disabled (string-only, identical to today).
- The CLI table additions match the ACTUAL landed behavior and exit codes (verified
  against `src/main.rs`): `--validate-rules` parse/schema error ⇒ exit 1; unknown
  callback names ⇒ warning, exit 0; no `rules.toml` ⇒ info, exit 0;
  `--rules-path <missing>` ⇒ error, exit 1; `--list-callbacks` always exit 0 with
  device/legacy/empty-table branches.
- `git diff --stat` = `docs/configuration.md` ONLY.

## User Persona (if applicable)

**Target User**: the power user editing a TOML file to drive host rules, and the
developer integrating a keyboard — both landing on `docs/configuration.md` as the
desktop-config reference.

**Use Case**: "I want my keyboard to switch layers / run callbacks based on the
active window WITHOUT reflashing. I open the Configuration guide, find the
`rules.toml` section, run `qmkonnect -c` to get a commented template (or copy the
annotated example), `qmkonnect --list-callbacks` to get my keyboard's real
callback names, `qmkonnect --validate-rules` to check my file, then click the tray
'Reload rules' to apply."

**User Journey**: read "File location" → copy the §9 example (or `qmkonnect -c`)
→ edit → `qmkonnect --validate-rules` (or `--rules-path` to target a draft) → fix
any parse error (the table tells them which keys are required) → reload. The
"Stack vs. replace" subsection tells them when the board's own rules keep running.

**Pain Points Addressed**: today `docs/configuration.md` has **zero** mention of
`rules.toml` (verified — 341 lines, unchanged). A user discovering the feature
(the tray "Reload rules" item, a `--validate-rules` flag, or a `rules.toml` file
on disk) has no in-page reference and must read the raw spec.

## Why

- **PRD §8(1) (`h2.86`)** names `rules.toml` as a first-class desktop config file
  (alongside `config.toml`) and **§9 (`h2.87`)** is its canonical schema. The
  Configuration page is the documented home for desktop config files — it must
  cover `rules.toml`.
- **HOST_RULES.md §8(6)** contracts the CLI flags (`--list-callbacks`,
  `--validate-rules`, `--rules-path`) and the `-c` seeding behavior — **now all
  LANDED** in `src/main.rs`. The existing "### CLI flags" table is the
  user-facing flag reference and must reflect them.
- **Item CONTRACT point 1** identifies `docs/configuration.md` as "the user-facing
  config contract."
- **Complements** P6.M1.T1.S2 (qmk-integration migration guide) and P6.M1.T1.S3
  (examples + troubleshooting recipes) — this task owns the **reference** layer;
  those own the **how-to/migration** layers, linked out via `{{ site.baseurl }}`.

## What

Three additive edits to `docs/configuration.md` (full detail in "All Needed
Context" → "Implementation Tasks"):

1. **EXTEND** the existing `### CLI flags` table — add 3 rows, annotate the `-c`
   row (no removals; the other 5 rows stay verbatim).
2. **ADD** a new `## Host Window Rules (rules.toml)` H2 section (three
   subsections) in the **desktop-side half** of the page, after the
   `### CLI flags` / `## Configuration Reference` block and before the first
   `---` separator that precedes `## QMK Firmware Configuration`.
3. (No other edits — `## Configuration Examples`, `## Configuration Reference`
   table, the firmware `DEFINE_*` half, and all other sections stay verbatim.)

### Success Criteria
- [ ] New `## Host Window Rules (rules.toml)` H2 exists, with `### File location`,
      `### Schema reference`, and `### Stack vs. replace (disable_firmware_config)`
      subsections, placed in the desktop-side half (before the `---` + firmware section).
- [ ] `### File location` states rules.toml lives "alongside `config.toml`, same
      directory, swapped filename", gives the per-OS paths (Linux
      `~/.config/qmk-notifier/rules.toml`, Windows `%APPDATA%\QMKonnect\rules.toml`,
      macOS `~/Library/Application Support/QMKonnect/rules.toml`), and states an
      absent file ⇒ host rules disabled (string-only; not an error).
- [ ] `### Schema reference` contains (a) a field TABLE matching
      `src/core/rules.rs` exactly, (b) the **verbatim** `HOST_RULES.md` §9
      annotated example in a ```toml fence, and (c) a "creating it" note that
      `qmkonnect -c` seeds a **commented** template (no-op if it exists) —
      uncomment to activate — OR hand-write.
- [ ] The field table marks `match` and `layer` as **required**; shows
      `disable_firmware_config` as `bool` in `[host]` and **optional** (absent ⇒
      inherits `[host]`) on each rule; shows `enable`/`disable`/`case_sensitive`
      as optional with their defaults.
- [ ] `### Stack vs. replace` correctly explains: global default under `[host]`
      (default `false` = stack); optional per-rule override; a window is "replace"
      iff **every** matched rule's effective flag is `true` (or board has no
      rules); no-match always clears the host layer + disables callbacks; no
      `rules.toml` ⇒ today's behavior (string-only).
- [ ] The `### CLI flags` table has 3 new rows (`--list-callbacks`,
      `--validate-rules [--rules-path <path>]`, `--rules-path <path>`) whose
      descriptions match the LANDED behavior (verified §0.3) and the `-c`/`--config`
      row notes it "also seeds a commented `rules.toml` template."
- [ ] New internal cross-links use `{{ site.baseurl }}/qmk-integration` and
      `{{ site.baseurl }}/examples` (NOT absolute URLs / `.md` extensions).
- [ ] ```toml fences are balanced (even count of ``` lines file-wide).
- [ ] `git diff --stat` = `docs/configuration.md` ONLY.

## All Needed Context

### Context Completeness Check

_Pass_: an agent with no prior knowledge of this codebase can implement this
using only this PRP + `research/notes.md`, because: (a) the EXACT current section
skeleton of `docs/configuration.md` (line-by-line, incl. the verbatim current
`### CLI flags` table) is in research §1; (b) the verbatim §9 annotated example to
embed is in §0.5 (copy byte-for-byte); (c) the schema field table is derivable 1:1
from the LANDED `src/core/rules.rs` structs (verified, §0.1) with the three traps
— `bool`-vs-`Option<bool>`, required `match`/`layer`, TOML key `match` — called
out in §4 G2/G3; (d) the `disable_firmware_config` semantics (global default /
per-rule override / AND decision / no-match-clear / backward-compat) are spelled
out in §0.6; (e) the EXACT CLI flag behaviors incl. output strings + exit codes
are tabulated in §0.3 — all VERIFIED against the LANDED `src/main.rs`; (f) the
seeded-template contract (`render_rules_body` is a COMMENTED §9 schema; `-c` is a
no-op if the file exists) is in §0.4; (g) the file-location contract (alongside
`config.toml`, per-OS table) is in §0.2; (h) the boundary with sibling tasks
(S2/S3/S4 — do NOT duplicate) is in §2; (i) the cross-link convention
(`{{ site.baseurl }}/slug`) is in §1.2; (j) design decisions D1-D7 and gotchas
G1-G7 are in §3/§4; (k) the validation commands (including RUNNING the real flags
to confirm the docs match) are in §5.

### Documentation & References

```yaml
# MUST READ — THIS task's full contract (verified structs, §9 example, CLI table
# with exact output strings + exit codes, disable_firmware_config semantics,
# current file skeleton, boundary, validation).
- file: plan/002_637d65b6e9b8/P6M1T1S1/research/notes.md
  why: "§0.1 = LANDED rules.rs structs (L68/94/130/167) — build the field table
        from these, EXACT. §0.2 = file-location contract (alongside config.toml).
        §0.3 = the 3 CLI flags + -c seeding, with VERIFIED output strings + exit
        codes (main.rs L125-364, list_callbacks L257, validate_rules L292,
        create_config L440, print_help L140). §0.4 = render_rules_body() is a
        COMMENTED §9 schema (mod.rs L185). §0.5 = the VERBATIM §9 example to embed.
        §0.6 = disable_firmware_config stack/replace semantics. §1 = line-by-line
        CURRENT configuration.md skeleton + verbatim CLI table. §2 = boundary
        (S2=qmk-integration migration, S3=examples/troubleshooting, S4=README+
        llms_full.txt regen). §3/§4 = D1-D7 design + G1-G7 gotchas. §5 = validation."

# MUST READ — the canonical schema + example (single source of truth).
- file: spec/HOST_RULES.md
  why: "§9 ('rules.toml Schema Reference') = the annotated example to embed VERBATIM.
        §8(6) = the CLI flag contract (now LANDED). §4 + §3 C10 = the
        disable_firmware_config stack-vs-replace + AND-decision semantics.
        §8(1) = the per-OS file location."
  section: "§9 (example), §8(6) (CLI), §8(1) (location), §4 + §3 C10 (stack/replace)"

# MUST READ — the LANDED data model (the field table must match these structs EXACTLY).
- file: src/core/rules.rs
  why: "RuleSet (L68) / HostDefaults (L94) / LayerRule (L130) / CallbackRule (L167) +
        parse_rules (L230) + get_rules_paths (L248) + evaluate (L322). The docs
        field table is generated 1:1 from these. Key facts: `match` + `layer` are
        REQUIRED (no #[serde(default)]); disable_firmware_config is `bool` in
        HostDefaults but `Option<bool>` per-rule (None => inherit host); layer is
        u8 and must be >= 224; Pattern is Single(String)|Parts(String,String)."
  pattern: "read the 4 struct definitions + get_rules_paths() delegating to
            platforms::get_config_paths().with_file_name('rules.toml')."
  gotcha: "the docs use the TOML KEY `match` (not the Rust field `pattern`);
           the TOML table-arrays are [[layer_rules]] / [[callback_rules]]."

# MUST READ — the LANDED CLI behavior (the docs MUST reproduce these exact strings).
- file: src/main.rs
  why: "list_callbacks() L257-291 (--list-callbacks output strings: no-device /
        capable-empty / capable-nonempty / legacy branches), validate_rules()
        L292-364 (--validate-rules path resolution + schema check + unknown-name
        warnings + the exact exit-0/exit-1 cases), create_config() L440-455 (now
        calls core::create_default_rules), print_help() L140-170 (the 3 new flag
        help lines). The docs CLI table + the 'creating it' note are accurate
        transcriptions of these."
  pattern: "every flag branch is `args.iter().any(|a| a == \"--flag\")`; value flags
            via parse_value_flag() (accepts `--flag v` and `--flag=v`)."
  gotcha: "--validate-rules is the ONLY fatal (exit-1) case — parse/schema error
           or --rules-path <missing>. Unknown callback names are warnings (exit 0).
           --list-callbacks is ALWAYS exit 0."

# MUST READ — the LANDED seeded template (the docs note must say COMMENTED).
- file: src/core/mod.rs
  why: "render_rules_body() L185-236 emits a FULLY-COMMENTED §9 schema (every
        active line prefixed `# `; header '# QMKonnect Host Rules (rules.toml)').
        create_default_rules() L246-276 is a no-op+message if the file exists,
        else writes the commented body. The docs must distinguish the UNCOMMENTED
        §9 reference example from the COMMENTED scaffold `-c` creates."
  section: "render_rules_body (L185), create_default_rules (L246)"
  gotcha: "the seeded file parses to all-defaults (host rules disabled) — verified
           by mod.rs test test_render_rules_body_parses_to_default_ruleset."

# MUST READ — the file THIS task edits (the ONLY file).
- file: docs/configuration.md
  why: "341-line Jekyll page (UNCHANGED). The verbatim current `### CLI flags`
        table (L215-219) is extended in place. The new H2 section is INSERTED
        after `## Configuration Reference`/`### CLI flags` and BEFORE the first
        `---` that precedes `## QMK Firmware Configuration`. Cross-links use
        `{{ site.baseurl }}/slug`."
  pattern: "front matter (layout: default, title, permalink: /configuration/);
            ```toml fences; `[text]({{ site.baseurl }}/page)` internal links;
            the existing `### CLI flags` markdown TABLE format."
  gotcha: "do NOT edit the firmware-side `## QMK Firmware Configuration` /
           `DEFINE_SERIAL_LAYERS` / `DEFINE_SERIAL_COMMANDS` half — that's legacy
           firmware config; host-rules is desktop-side. do NOT touch the
           `## Configuration Reference` config.toml keys table — add the rules.toml
           field table INSIDE the new section. do NOT regenerate llms_full.txt."

# REFERENCE — the LANDED handshake (confirms --list-callbacks/--validate-rules wiring).
- file: src/core/notifier.rs
  why: "is_device_connected() L171, perform_handshake() L265, host_capable() L441,
        callback_names() L448. Confirms the handshake the CLI flags call is real
        and merged (P4.M2.T1.S1 Complete). The docs only need its user-facing
        contract (capable vs legacy vs no-device), not its internals."
```

### Current Codebase tree (relevant subset)

```bash
docs/
  configuration.md     # ← THIS TASK EDITS (the ONLY file): + new H2 section,
                       #   +3 CLI-table rows, -c annotation. Jekyll page (kramdown/rouge).
  qmk-integration.md   # P6.M1.T1.S2 (host-rules migration guide). UNCHANGED (linked OUT).
  examples.md          # P6.M1.T1.S3 (host-rules recipes). UNCHANGED (linked OUT).
  troubleshooting.md   # P6.M1.T1.S3. UNCHANGED (linked OUT).
  README.md            # repo README. P6.M1.T1.S4. UNCHANGED.
  llms_full.txt        # GENERATED concatenation of docs/*.md. P6.M1.T1.S4 regenerates
                       #   it AFTER all doc edits land. NOT touched by this task.
  _config.yml          # Jekyll: baseurl "/qmkonnect", markdown kramdown, highlighter rouge.
src/core/
  rules.rs             # P3.M1 LANDED (RuleSet L68, HostDefaults L94, LayerRule L130,
                       #   CallbackRule L167, parse_rules L230, get_rules_paths L248,
                       #   evaluate L322). READ-ONLY source of truth for the field table.
  mod.rs               # P5.M1.T1.S1 LANDED (render_rules_body L185, create_default_rules
                       #   L246). READ-ONLY source of truth for the seeded-template note.
src/main.rs            # P5.M1.T1.S1 LANDED (list_callbacks L257, validate_rules L292,
                       #   create_config L440, print_help L140). READ-ONLY source of truth
                       #   for the CLI flag descriptions.
spec/HOST_RULES.md     # §9 example + §8(6) CLI + §4/§3-C10 semantics. READ-ONLY.
```

### Desired Codebase tree with files to be changed

```bash
docs/configuration.md    # MODIFIED: + "## Host Window Rules (rules.toml)" H2 section
                         #   (3 subsections: File location / Schema reference w/ verbatim
                         #   §9 example + field table / Stack-vs-replace), +3 rows in the
                         #   "### CLI flags" table, -c row annotation, cross-links out.
# EVERYTHING ELSE UNCHANGED. No Rust, no Cargo.toml, no other docs/, no llms_full.txt.
```

### Known Gotchas of our codebase & Library Quirks

```markdown
<!-- CRITICAL (G1 — the build PASSES; use it). cargo build --bin qmkonnect => EXIT 0
     today. RUN `cargo run -- --help`, `--validate-rules`, `-c` to confirm the docs
     match the real output strings (Validation Level 3). -->

<!-- CRITICAL (G2 — field table must match src/core/rules.rs EXACTLY). Three traps:
       (a) disable_firmware_config is `bool` in [host] but `Option<bool>` per-rule
           (absent => inherits [host]); the table must show BOTH.
       (b) `match` and `layer` are REQUIRED (no default) — mark them required.
       (c) the TOML KEY is `match` (Rust field is `pattern`); docs use the TOML key. -->

<!-- GOTCHA (G3 — the required layer key is `layer`, u8, >= 224). Not layer_id.
     255 = clear (None). State the >= 224 range. -->

<!-- GOTCHA (G4 — `match` accepts a string OR a 2-element array). String "foo" =>
     class-only; array ["cls","ttl"] => class+title (== firmware WT()). 1-/3-element
     arrays are parse errors. The §9 example shows both forms. -->

<!-- GOTCHA (G5 — the stack/replace decision is per-WINDOW, via an AND over matched
     rules). One non-disabling matched rule => whole window is "stack". -->

<!-- GOTCHA (G6 — Jekyll Markdown). Use ```toml fences (rouge highlighter).
     Internal links: `{{ site.baseurl }}/slug` (NOT https, NOT .md). The permalink
     is /configuration/ (in front matter — don't touch it). -->

<!-- GOTCHA (G7 — scope). Edit ONLY docs/configuration.md. Do NOT add the host-rules
     migration guide (S2), recipes (S3), or regenerate llms_full.txt (S4). LINK out;
     don't reproduce them. Don't edit the firmware DEFINE_* half or the config.toml
     keys table. -->
```

## Implementation Blueprint

### Document structure (the deliverable)

The new content. Exact wording is in the Implementation Tasks below; this is the
shape. Copy the §9 example (§0.5) and the struct facts (§0.1) **verbatim** from
`research/notes.md` — do not paraphrase the schema or the example.

#### A. The new `## Host Window Rules (rules.toml)` section

```markdown
## Host Window Rules (`rules.toml`)

Host rules let your keyboard switch layers and run callbacks based on the active
window — **without reflashing**. They live in an editable `rules.toml` file on
your computer and are matched by QMKonnect on the host. Host rules **stack on top
of** your board's existing `DEFINE_SERIAL_LAYERS` / `DEFINE_SERIAL_COMMANDS`
rules (the board's rules always run first, then host rules apply on top), unless
a rule opts into "replace" mode (see
[Stack vs. replace](#stack-vs-replace-disable_firmware_config) below).

> **Firmware prerequisite.** Host rules require firmware that advertises the
> typed-command capability (`proto_ver == 2` + the `APPLY_HOST_CONTEXT` feature
> flag). With legacy firmware (or while the keyboard is disconnected) QMKonnect
> silently falls back to today's string-only behavior — your board's existing
> rules keep working unchanged. See the
> [QMK Integration Guide]({{ site.baseurl }}/qmk-integration) for the firmware
> side (including the `DEFINE_HOST_CALLBACKS` callback registry and migrating
> `DEFINE_*` rules to the host).

### File location

`rules.toml` lives **in the same directory as `config.toml`**, with the filename
swapped from `config.toml` to `rules.toml`:

| OS | `rules.toml` path |
| --- | --- |
| Linux | `~/.config/qmk-notifier/rules.toml` (also honors `$XDG_CONFIG_HOME` and `/etc/qmk-notifier/`) |
| Windows | `%APPDATA%\QMKonnect\rules.toml` |
| macOS | `~/Library/Application Support/QMKonnect/rules.toml` |

The file is **optional**. If it is absent, host rules are disabled and QMKonnect
behaves exactly as it does today (string-only). An empty or all-default
`rules.toml` is also a no-op — host rules only activate when a rule matches the
active window.

### Schema reference

`rules.toml` has one optional table and two table-arrays:

| Section / Key | Required | Default | Description |
| --- | :---: | --- | --- |
| `[host]` table | no | — | Global host defaults. |
| `[host] disable_firmware_config` | no | `false` | Global stack/replace default. `false` = the board runs its own rules too (**stack**); `true` = the host takes over (**replace**). Per-rule `disable_firmware_config` overrides this. See [Stack vs. replace](#stack-vs-replace-disable_firmware_config). |
| `[[layer_rules]]` table-array | no | `[]` | Layer rules. **First match wins**; one host layer is active at a time. |
| `[[layer_rules]] match` | **yes** | — | Window pattern. A bare string (`"alacritty"`) matches the **window class only**; a two-element array (`["*chrome*", "*youtube*"]`) matches **class and title** (equivalent to the firmware `WT(class, title)`). Supports `*`, `^`, `$`, `+`, character classes (`\d \w \s …`), and `.` — full parity with the firmware matcher. |
| `[[layer_rules]] layer` | **yes** | — | The host layer number to activate. Must be **≥ 224** (the host layer range; `255` clears the layer). |
| `[[layer_rules]] case_sensitive` | no | `false` | Whether `match` is case-sensitive. |
| `[[layer_rules]] disable_firmware_config` | no | inherits `[host]` | Per-rule stack/replace override. Absent ⇒ uses the `[host]` default. |
| `[[callback_rules]]` table-array | no | `[]` | Callback rules. **All matches fire.** Names come from your keyboard's callback registry (run `qmkonnect --list-callbacks` to see them). |
| `[[callback_rules]] match` | **yes** | — | Window pattern (same form as `[[layer_rules]] match`). |
| `[[callback_rules]] enable` | no | `[]` | Callback names to enable on focus-in. |
| `[[callback_rules]] disable` | no | `[]` | Callback names to force off (explicit exclusion). Focus-out `on_disable` also fires automatically when a callback leaves the active set. |
| `[[callback_rules]] case_sensitive` | no | `false` | Whether `match` is case-sensitive. |
| `[[callback_rules]] disable_firmware_config` | no | inherits `[host]` | Per-rule stack/replace override. |

**Creating the file.** Run `qmkonnect -c` to seed a fully-commented `rules.toml`
template alongside your `config.toml` (it is a no-op if `rules.toml` already
exists). The seeded template is the schema below with every active line commented
out (`#`), so it parses to an all-default ruleset — host rules stay disabled until
you uncomment and edit the lines. You can also hand-write the file. After editing,
validate it with `qmkonnect --validate-rules` (see [CLI flags](#cli-flags)).

Here is the complete annotated example (from `spec/HOST_RULES.md` §9) — what an
**active** `rules.toml` looks like once you uncomment and edit it:

```toml
# rules.toml — host-side window rules.
# disable_firmware_config chooses, per window, whether the board runs its own
# rules (stack) or is cleared and driven solely by the host (replace). Global
# default under [host]; per-rule override below. Host layers are >= 224.
# Run `qmkonnect --validate-rules` after editing.

[host]
disable_firmware_config = false   # global default: false = stack (board runs), true = replace
# On no match the host layer is always cleared and all host callbacks disabled.

# Layer rules: FIRST match wins. One host layer active at a time (>= 224).
[[layer_rules]]
match = "alacritty"                       # class-only pattern
layer = 224
disable_firmware_config = true           # optional override (default inherits [host])

[[layer_rules]]
match = ["*chrome*", "*youtube*"]         # [class_pattern, title_pattern] (== WT())
layer = 225
case_sensitive = false                    # optional, default false

# Callback rules: ALL matches fire. Names come from the keyboard's registry
# (run `qmkonnect --list-callbacks` to see them). The disable list is an
# explicit-exclusion override; focus-out on_disable fires automatically via the
# desired-set diff.
[[callback_rules]]
match = "neovide"
enable = ["vim_lazy", "disable_vim"]      # run on focus-in
disable = ["vim_lazy"]                    # optional: force-off override

[[callback_rules]]
match = ["*chrome*", "*claude*"]
enable = ["vim_lazy", "disable_vim"]
disable_firmware_config = true           # for this window, skip the string -> board can't match
```

For ready-made `rules.toml` recipes, see the
[Examples]({{ site.baseurl }}/examples).

### Stack vs. replace (`disable_firmware_config`)

`disable_firmware_config` decides, **per window**, whether your board runs its
own rules:

- **Stack** (`false`, the default): the board runs its own `DEFINE_*` rules.
  QMKonnect sends the window string first (the board matches it and runs its
  layer/command rules), then applies the host layer **on top** and syncs the host
  callbacks. Board layer first → host layer on top; board callbacks first → host
  callbacks after.
- **Replace** (`true`): the host takes over for this window. QMKonnect sends
  **no** string (so the board can't match), and the firmware clears its own board
  layer/command before applying only the host layer + host callbacks.

The value is resolved per rule:

- The **global default** is `[host] disable_firmware_config` (default `false`).
- Each `[[layer_rules]]` / `[[callback_rules]]` may set an optional
  `disable_firmware_config` to override it. **Absent ⇒ inherits the `[host]`
  default.**
- A rule's **effective** flag is its override if set, else the `[host]` default.

The per-window decision is an **AND** over all rules that matched that window:

- The window is **replace** iff **every** matched rule's effective flag is `true`
  — *or* the board has no rules of its own.
- If even **one** matched rule is non-disabling, the window is **stack**.
- **No match:** the host layer is always cleared (`255`) and all host callbacks
  are disabled. (There is no "keep" option.)

**Rule of thumb:** set `disable_firmware_config = true` on a rule when you want
the host to fully own that window (e.g. a browser site where the board's app
rules would interfere); leave it `false` when you want the board's own rules to
keep running underneath.
```

> *(The heading anchor `#stack-vs-replace-disable_firmware_config` is what the
> two `[Stack vs. replace](#…)` links above resolve to under kramdown's
> auto-anchor slugification: lowercase, spaces → hyphens, backticks/parens
> stripped. The `[CLI flags](#cli-flags)` anchor resolves to the existing
> "### CLI flags" heading. Render once with Jekyll and copy the real anchors from
> the browser if unsure.)*

#### B. The extended `### CLI flags` table

Add the three new rows and annotate `-c`. The six existing rows are kept verbatim
except the `-c`/`--config` description gains a trailing clause. Descriptions match
the LANDED `src/main.rs` behavior (research §0.3):

```markdown
### CLI flags

| Flag | Description |
| --- | --- |
| `-c`, `--config` | Create a default (commented-out) `config.toml` **and** a commented `rules.toml` template (no-op if either exists). |
| `-r`, `--reload` | Re-read the config and write the matching udev rule (Linux; requires root). |
| `-l`, `--list` | List the platforms supported by this build. |
| `--list-devices` | List connected HID devices (VID/PID discovery). |
| `--list-callbacks` | Handshake the connected keyboard and print its callback name→id table (sorted by id). With legacy firmware prints a string-only-mode notice; with no board prints a no-device notice (always exits 0). |
| `--validate-rules` [`--rules-path <path>`] | Parse `rules.toml` and report schema/callback-name errors. A parse/schema error (or a `--rules-path` that doesn't exist) exits non-zero; unknown callback names are warnings (exit 0); a missing `rules.toml` is informational (exit 0). Use `--rules-path` to validate a file outside the default location. |
| `--rules-path <path>` | Override the `rules.toml` location (used with `--validate-rules`). |
| `-v`, `--verbose` | Enable verbose logging. |
| `-h`, `--help` | Show help. |
```

### Implementation Tasks (ordered by dependencies)

```yaml
Task 1: READ the current docs/configuration.md + research/notes.md + spec/HOST_RULES.md §9
  - READ docs/configuration.md in full (341 lines) to confirm the exact skeleton
    and locate the two anchors: the "### CLI flags" table (≈L215) and the first
    "---" separator preceding "## QMK Firmware Configuration" (≈L248).
  - READ plan/002_637d65b6e9b8/P6M1T1S1/research/notes.md §0.1 (structs),
    §0.3 (CLI strings + exit codes), §0.4 (seeded template), §0.5 (§9 example),
    §0.6 (semantics), §1 (skeleton + CLI table).
  - READ spec/HOST_RULES.md §9 to confirm the example is byte-identical to §0.5.
  - VERIFY the schema facts against src/core/rules.rs (4 structs: required
    match/layer; bool vs Option<bool> disable_firmware_config; layer u8 >= 224)
    and the CLI behavior against src/main.rs (list_callbacks L257, validate_rules
    L292, create_config L440, print_help L140).
  - GOTCHA G2: the field table is generated 1:1 from the structs — do not invent fields.

Task 2: EDIT the "### CLI flags" table (extend in place — Task B above)
  - FIND the current table (6 rows: -c/-r/-l/--list-devices/-v/-h).
  - REPLACE the `-c`/`--config` row's description with the extended wording
    ("… and a commented rules.toml template (no-op if either exists).").
  - APPEND three new rows AFTER `--list-devices` (grouping the host-rules flags
    together): `--list-callbacks`, `--validate-rules [--rules-path <path>]`,
    `--rules-path <path>` — exact descriptions in Task B's table block.
  - KEEP the other 5 rows verbatim (only -c's description changes).
  - GOTCHA G6: keep the markdown TABLE format identical (`| Flag | Description |`).
  - VERIFY: grep -n 'list-callbacks\|validate-rules\|rules-path' docs/configuration.md -> 3 flags present.

Task 3: INSERT the new "## Host Window Rules (rules.toml)" section (Task A above)
  - INSERT the entire new H2 section (intro blockquote + File location + Schema
    reference + Stack vs. replace) AFTER the "### CLI flags" table / end of the
    "## Configuration Reference" block, and BEFORE the first "---" separator
    that precedes "## QMK Firmware Configuration".
  - COPY the §9 annotated example BYTE-FOR-BYTE from research/notes.md §0.5
    (or spec/HOST_RULES.md §9) into a ```toml fence inside "Schema reference".
    Do NOT paraphrase the comments — they are part of the spec.
  - BUILD the field table from research/notes.md §0.1 (the LANDED structs):
    mark `match` and `layer` REQUIRED; show disable_firmware_config as `bool`
    in [host] and optional (inherits [host]) per-rule; show enable/disable/
    case_sensitive defaults.
  - WRITE the "Creating the file" note: qmkonnect -c seeds a COMMENTED template
    (every active line `#`-prefixed) so a fresh install is inert; no-op if it
    exists; or hand-write. Cross-link to "#cli-flags".
  - INCLUDE the per-OS File-location table (Linux/Windows/macOS) and the
    "absent => disabled (not an error)" note.
  - WRITE the Stack-vs-replace subsection from research/notes.md §0.6: global
    default / per-rule override / effective-flag resolution / AND decision /
    no-match-clear / rule-of-thumb.
  - ADD cross-links via {{ site.baseurl }}: /qmk-integration (firmware +
    migration) and /examples (recipes). Do NOT duplicate their content.
  - GOTCHA G3: the required layer key is `layer` (>= 224), not layer_id.
  - GOTCHA G4: `match` accepts string OR 2-element array (state both forms).
  - GOTCHA G5: the stack/replace decision is per-WINDOW (AND over matched rules).
  - GOTCHA G7: do NOT edit the firmware DEFINE_* half or the config.toml keys table.
  - VERIFY: grep -c 'rules.toml' docs/configuration.md (>= 6); grep 'disable_firmware_config'
    (field table + semantics); grep 'alacritty' (the §9 example present).

Task 4: VALIDATE (Markdown + RUN the real flags to confirm the docs match)
  - RUN the §5 validation commands from research/notes.md:
      git diff --stat                 # expect docs/configuration.md ONLY
      grep -c 'rules.toml' ...        # >= 6
      grep -n 'disable_firmware_config\|--list-callbacks\|--validate-rules\|--rules-path\|alacritty\|stack\|replace' ...
      test $(( $(grep -c '^```' ...) % 2 )) -eq 0   # balanced fences
      grep -oE '\{\{ site\.baseurl \}\}/[a-z-]+' ... | sort -u   # link slugs
  - RUN the real flags to confirm the docs match the LANDED behavior:
      cargo run --bin qmkonnect -- --help | grep -E 'list-callbacks|validate-rules|rules-path'
      cargo run --bin qmkonnect -- --validate-rules   # no rules.toml => "Nothing to validate." exit 0
      cargo run --bin qmkonnect -- -c                 # inspect the seeded commented template
  - (Optional) Jekyll build if ruby/bundle present: cd docs && bundle exec jekyll build.
  - EYEBALL: render the page (or read the markdown) and confirm the field table
    matches src/core/rules.rs and the §9 example is intact.
```

### Implementation Patterns & Key Details

```markdown
<!-- PATTERN: the existing "### CLI flags" markdown table — match its format exactly
     when adding rows. The `--validate-rules [--rules-path <path>]` row uses square
     brackets (no pipe), so no cell escaping needed. -->

<!-- PATTERN: ```toml fences for TOML (rouge highlights them). The existing page
     already uses ```toml for the config.toml template (≈L48) and ```bash (≈L43).
     Match that. Do NOT use ``` (unlabeled) for TOML. -->

<!-- PATTERN: internal links are `[text]({{ site.baseurl }}/slug)` — see every
     existing link (e.g. {{ site.baseurl }}/qmk-integration). The slug has NO
     trailing .md and NO leading / before the baseurl expansion. For same-page
     anchors use `(#heading-slug)` (kramdown auto-slugs headings). -->

<!-- KEY DETAIL: distinguish the UNCOMMENTED §9 reference example (shown in the docs)
     from the COMMENTED scaffold `qmkonnect -c` creates (render_rules_body in mod.rs
     L185 prefixes every active line with `# `). Both are the §9 schema; one is
     active, one is inert-until-uncommented. The "Creating the file" note says so. -->

<!-- KEY DETAIL: the §9 example's final comment line ends with "board can't match".
     Copy it verbatim — it explains WHY disable_firmware_config=true skips the
     string (the board lane can't match without a string). -->

<!-- KEY DETAIL: the heading anchors — "### CLI flags" => #cli-flags (existing);
     "### Stack vs. replace (`disable_firmware_config`)" =>
     #stack-vs-replace-disable_firmware_config (kramdown: lowercase, spaces->hyphens,
     backticks/parens dropped). Render once to confirm. -->
```

### Integration Points

```yaml
DOCUMENT STRUCTURE: the new H2 is inserted in the desktop-side half of the page,
  between "### CLI flags" (end of "## Configuration Reference") and the first "---"
  that precedes "## QMK Firmware Configuration". No other section is moved/removed.

CROSS-LINKS (added — Jekyll {{ site.baseurl }}):
  - {{ site.baseurl }}/qmk-integration  (firmware setup + host-rules migration — S2)
  - {{ site.baseurl }}/examples          (rules.toml recipes — S3)
  - same-page anchor #cli-flags          (from the "Creating the file" note)
  - same-page anchor #stack-vs-replace-disable_firmware_config (from the table + intro)

DOWNSTREAM CONSUMERS:
  - P6.M1.T1.S4 (README.md + llms_full.txt regen): llms_full.txt is a GENERATED
    concatenation of docs/*.md. This task's configuration.md edits will be picked
    up when S4 regenerates it AFTER all doc edits land. Do NOT regenerate it here.

BUILD: cargo build --bin qmkonnect => EXIT 0 today (all deps LANDED). Jekyll
  (docs/_config.yml: markdown kramdown, highlighter rouge, baseurl "/qmkonnect")
  renders the page on GitHub Pages. An optional local `bundle exec jekyll build`
  validates Liquid/fence syntax; RUNNING the CLI flags (`cargo run -- …`)
  confirms the docs match the real output.
```

## Validation Loop

### Level 1: Markdown consistency (Immediate Feedback)

```bash
cd /home/dustin/projects/qmkonnect

# Scope gate: ONLY docs/configuration.md changed.
git diff --stat
# EXPECT: docs/configuration.md ONLY.

# The new content landed (schema + flags + semantics).
grep -c 'rules.toml' docs/configuration.md                  # >= 6
grep -n 'disable_firmware_config' docs/configuration.md     # field table + semantics section
grep -n -- '--list-callbacks\|--validate-rules\|--rules-path' docs/configuration.md  # 3 new flags
grep -n 'alacritty' docs/configuration.md                   # the §9 example marker
grep -ni 'stack\|replace' docs/configuration.md             # the semantics subsection

# Balanced code fences (even count of ``` lines).
test $(( $(grep -c '^```' docs/configuration.md) % 2 )) -eq 0 \
  && echo "fences balanced" || echo "UNBALANCED FENCES — fix before committing"

# Internal link slugs are well-formed (no .md, no double slashes).
grep -oE '\{\{ site\.baseurl \}\}/[a-z-]+' docs/configuration.md | sort -u
```

### Level 2: Schema-fidelity cross-check (Component Validation)

```bash
cd /home/dustin/projects/qmkonnect

# The field table in the new section must match src/core/rules.rs. Eyeball these
# four facts against the rendered "Schema reference" table:
#   1. `match` and `layer` are marked REQUIRED (they have no #[serde(default)]).
grep -nE 'yes' docs/configuration.md          # the "Required" column rows for match/layer
#   2. disable_firmware_config appears as BOTH bool ([host]) and optional (per-rule).
grep -c 'disable_firmware_config' docs/configuration.md   # >= 4 (table host-row, table rule-rows, semantics)
#   3. layer is documented as >= 224.
grep -nE '224|>= 224' docs/configuration.md
#   4. `match` accepts string OR 2-element array.
grep -nE 'class-only|class and title|WT\(' docs/configuration.md

# The §9 example is intact (both layer rules + both callback rules + the [host] block).
grep -A2 'alacritty' docs/configuration.md
grep -A2 '\*chrome\*' docs/configuration.md
grep -A2 'neovide' docs/configuration.md
```

### Level 3: Run the real flags (System Validation — confirms docs match LANDED code)

```bash
cd /home/dustin/projects/qmkonnect

# The build passes today — use it to confirm the docs match the real output.
cargo run --bin qmkonnect -- --help | grep -E 'list-callbacks|validate-rules|rules-path'
# EXPECT: the 3 help lines (print_help L162-164). The docs CLI table mirrors them.

cargo run --bin qmkonnect -- --validate-rules
# EXPECT: "No rules.toml found (host rules disabled). Nothing to validate." + exit 0
#         (no rules.toml present in CI/dev env). The docs say exactly this.

cargo run --bin qmkonnect -- -c
# EXPECT: seeds ~/.config/qmk-notifier/config.toml + rules.toml. Inspect the seeded
#         rules.toml: header "# QMKonnect Host Rules (rules.toml)", every active line
#         commented (`#`). The docs "Creating the file" note describes this.
head -8 ~/.config/qmk-notifier/rules.toml 2>/dev/null

# (Optional) Jekyll build if ruby/bundle available — catches Liquid/fence errors.
cd docs && bundle exec jekyll build 2>&1 | grep -iE 'error|warning' || echo "jekyll build clean"
# Then open _site/configuration/index.html and confirm:
#   - the new "Host Window Rules" H2 appears in the desktop-side half,
#   - the §9 TOML example is syntax-highlighted (rouge) and intact,
#   - the field table renders with the Required column,
#   - the cross-links to /qmk-integration/ and /examples/ resolve,
#   - the same-page anchors (#cli-flags, #stack-vs-replace-disable_firmware_config) work.
```

### Level 4: Cross-reference + boundary gate (Domain-Specific)

```bash
cd /home/dustin/projects/qmkonnect

# Boundary: this task did NOT duplicate the sibling tasks' content.
grep -c 'DEFINE_HOST_CALLBACKS' docs/configuration.md   # 0 or only a passing mention —
                                                       # the migration GUIDE is S2 (qmk-integration.md)
grep -c 'llms_full' docs/configuration.md              # 0 — S4 owns regeneration
git diff --name-only                                   # ONLY docs/configuration.md

# The CLI flag exit-code semantics match the LANDED src/main.rs (validate_rules L292-364).
# Confirm the --validate-rules row states: parse/schema error => non-zero; unknown
# callback names => warning/exit 0; missing file => info/exit 0; --rules-path <missing>
# => error non-zero. And --list-callbacks is always exit 0.
grep -A1 'validate-rules\|list-callbacks' docs/configuration.md
```

## Final Validation Checklist

### Technical Validation
- [ ] `git diff --stat` = `docs/configuration.md` ONLY (scope gate).
- [ ] ```toml / ``` fences balanced file-wide (Level 1).
- [ ] All `{{ site.baseurl }}/slug` cross-links are well-formed (Level 1).
- [ ] `cargo run -- --help` / `--validate-rules` / `-c` output matches the docs
      (Level 3 — the build passes today; use it).
- [ ] (If run) `bundle exec jekyll build` reports no Liquid/fence errors (Level 3).

### Feature Validation (contract fidelity — item CONTRACT points 1-5 + §9 example)
- [ ] New `## Host Window Rules (rules.toml)` H2 with `### File location`,
      `### Schema reference`, `### Stack vs. replace` subsections, in the
      desktop-side half (before `---` + firmware section). [CONTRACT 1,2,3]
- [ ] File location states "alongside config.toml, swapped filename" + per-OS
      paths (Linux/Windows/macOS) + "absent ⇒ disabled (not an error)". [CONTRACT 1]
- [ ] Schema reference has the field TABLE matching `src/core/rules.rs` exactly
      (required match/layer; bool-vs-Option<bool> disable_firmware_config; layer ≥ 224;
      match string-or-2-array). [CONTRACT 2]
- [ ] The **complete annotated `HOST_RULES.md` §9 example** is embedded verbatim
      in a ```toml fence. [CONTRACT: "Include the complete annotated example"]
- [ ] `disable_firmware_config` stack/replace semantics are correct: global
      `[host]` default (false=stack); optional per-rule override (absent ⇒ inherits);
      window is replace iff EVERY matched rule effective=true (or no board rules);
      no-match always clears; absent rules.toml ⇒ today's behavior. [CONTRACT 3]
- [ ] `### CLI flags` table has the 3 new rows + `-c` annotated (seeds rules.toml),
      matching the LANDED `src/main.rs` behavior/exit codes. [CONTRACT 4,5]
- [ ] `-c` seeding is also mentioned in the new "Schema reference → creating it"
      note (and notes the template is COMMENTED). [CONTRACT 5]

### Code Quality Validation (documentation)
- [ ] Field table, §9 example, and semantics match the LANDED code + spec (no drift).
- [ ] Markdown table / fence / link formatting is consistent with the rest of the file.
- [ ] No duplication of S2 (migration) / S3 (recipes) / S4 (llms_full.txt) content —
      only cross-links out.
- [ ] New section is discoverable (intro blockquote, cross-links from CLI table and
      from the firmware-prerequisite note).

### Documentation & Deployment
- [ ] Plain-language explanations (esp. stack/replace) for a non-spec-reader audience.
- [ ] Cross-links point at the right sibling pages (qmk-integration, examples).
- [ ] No mention of internal pipeline state — the docs describe the user-facing feature.

---

## Anti-Patterns to Avoid

- ❌ Don't **paraphrase the §9 example** or its comments. Copy it byte-for-byte from
      `research/notes.md` §0.5 / `spec/HOST_RULES.md` §9 — it IS the spec.
- ❌ Don't **invent or rename schema fields**. The field table is generated 1:1 from
      `src/core/rules.rs`. The TOML key is `match` (not `pattern`); the layer key is
      `layer` (not `layer_id`); `disable_firmware_config` is `bool` in `[host]` and
      `Option<bool>` per-rule (G2/G3).
- ❌ Don't **mark optional keys as required** (or vice versa). `match` + `layer` are
      required; `enable`/`disable`/`case_sensitive`/`disable_firmware_config` default.
- ❌ Don't **describe the stack/replace decision as per-rule**. It's per-WINDOW (an
      AND over all matched rules); one non-disabling matched rule makes the whole
      window "stack" (G5).
- ❌ Don't **create a second CLI flags table** or move the existing one. Extend the
      existing `### CLI flags` table in place (3 new rows + the `-c` annotation).
- ❌ Don't **claim CLI behaviors that differ from the LANDED code**. The build passes;
      verify each flag description against `src/main.rs` (list_callbacks L257,
      validate_rules L292) or by running `cargo run -- --<flag>`. (E.g. `--list-callbacks`
      is ALWAYS exit 0; `--validate-rules` is the only exit-1 case, on parse/schema
      error or a missing explicit `--rules-path`.)
- ❌ Don't **describe the `-c` seeded template as uncommented**. `render_rules_body()`
      (mod.rs L185) emits a FULLY-COMMENTED schema — a fresh install is inert. The docs
      show the UNCOMMENTED §9 example as the *reference* and note `-c` seeds the
      *commented* version (G/D5).
- ❌ Don't **duplicate the sibling tasks' content** — the migration guide is S2
      (qmk-integration.md), recipes are S3 (examples.md/troubleshooting.md),
      llms_full.txt regeneration is S4. LINK out; don't reproduce (G7).
- ❌ Don't **regenerate `docs/llms_full.txt`**. It's a generated concatenation owned
      by P6.M1.T1.S4, regenerated after all doc edits land.
- ❌ Don't **edit the firmware-side half** (`## QMK Firmware Configuration`,
      `DEFINE_SERIAL_LAYERS`/`DEFINE_SERIAL_COMMANDS`) or the `config.toml` keys
      table. Host rules are desktop-side; add the new field table INSIDE the new
      section (G7).
- ❌ Don't use **absolute URLs or `.md` extensions** for internal links. Use
      `{{ site.baseurl }}/slug` to match every existing link in the file (G6).
- ❌ Don't **edit any file other than `docs/configuration.md`** — no Rust, no Cargo,
      no other docs, no spec, no plan files (except this item's own PRP/research).

---

## Confidence Score: 9.5/10

This is a **well-bounded documentation task** with every input VERIFIED against the
LANDED code (not a spec contract): the schema field table is generated 1:1 from
`src/core/rules.rs` (P3.M1 Complete, structs at L68/94/130/167); the §9 annotated
example to embed is verbatim in `research/notes.md` §0.5; the CLI flag descriptions
+ exit codes are transcribed from the actual `src/main.rs` (`list_callbacks` L257,
`validate_rules` L292, `create_config` L440, `print_help` L140 — all P5.M1.T1.S1
LANDED); the seeded-template contract (COMMENTED §9 schema) is from `mod.rs`
`render_rules_body` L185; and the build PASSES today, so the implementer can RUN the
real flags to confirm the docs match (Validation Level 3 — a luxury unavailable in
the first research pass when the CLI code hadn't landed). The current
`docs/configuration.md` skeleton and the exact `### CLI flags` table are captured
verbatim in §1; the `disable_firmware_config` semantics are spelled out in §0.6;
the per-OS file location is in §0.2; and the boundary with S2/S3/S4 is explicit.
The only residual risk (the 0.5-point reservation) is the **kramdown heading-anchor
slug** for `### Stack vs. replace (\`disable_firmware_config\`)` — its auto-anchor
(`#stack-vs-replace-disable_firmware_config`) and the `#cli-flags` anchor are best
confirmed by one local Jekyll render, but a wrong anchor only breaks a same-page
jump, not the content, and is trivially fixed. Scope is cleanly one file; the
anti-patterns guard against the two most likely slips (paraphrasing the
schema/example, and duplicating sibling-task content). No code/build risk remains
— the feature the docs describe is already shipped and passing.