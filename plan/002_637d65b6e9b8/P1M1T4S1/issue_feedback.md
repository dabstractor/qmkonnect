# Implementation Issue Feedback (Attempt 1/3)

The previous implementation attempt encountered an issue that requires re-planning.

## Issue Details

HALTED at the pre-flight gate (Task 1) per the PRP's failure protocol ('surface it and stop'). I made NO changes — Cargo.toml is still 0.2.1, no commit, no tag, no push. Two independent input-precondition failures block this release chore:\n\n(1) DIRTY TREE — T3.S2's work is uncommitted. `git status` shows `M README.md` and `M src/lib.rs`. The README.md diff is exactly T3.S2's Edit H (the Programmatic-Usage example rewritten to `match` on `CommandResponse`), and src/lib.rs is a 3-line comment update in the tests module. The PRP's coordination directive is explicit: 'If git status shows README.md modified (uncommitted T3.S2 work), coordinate: T3.S2 must commit BEFORE the T4.S1 bump commit so the tag points at a tree with the README already updated.' These are NOT my files to commit (ownership boundary: README is T3.S2's; the bump commit must be version-only). The untracked `plan/001_b92a9b2b603f/P1M4T1S1/` is unrelated crate-local planning.\n\n(2) FLAKY PRE-FLIGHT TEST GATE — `cargo test --lib` (the PRP's literal gate) fails intermittently with a heap-corruption abort. Reproduction: observed 1 SIGABRT across ~24 default-parallel runs; the abort is `corrupted size vs. prev_size while consolidating` (signal 6), firing AFTER all 57 test cases print `ok`, during test-binary teardown. With `--test-threads=1` the suite is deterministic green (57 passed; 0 failed every time). Root cause (T1–T3 code, not this chore): the global `static DEVICE_CACHE: LazyLock<Mutex<Option<DeviceCache>>>` in src/core.rs:571 holds an `HidApi` + `Vec<HidDevice>`; the Mutex guards runtime access but NOT process-teardown ordering, so when multiple parallel test threads' lingering state interacts with hidapi-2.4.1's C-level `hid_exit`/handle-close during LazyLock Drop, glibc's tcache checker intermittently flags the resulting double/out-of-order free. The PRP forbids me from fixing this ('the bug is in T1-T3 CODE, not this release chore — do NOT fix it here; surface it and stop').\n\nREQUIRED before this chore can run:\n  • T3.S2 must commit its README.md + src/lib.rs changes (so the tree is clean and the bump commit can be version-only, per the v0.2.1 convention).\n  • Decision needed on the hidapi-teardown flake: either (a) make the crate's test teardown thread-safe (join/drop device handles explicitly before process exit, or gate HID-touching tests behind a serial mutex), or (b) formally accept the flake and change the PRP's gate to `cargo test --lib -- --test-threads=1`. Tagging flaky code as-is would ship a release whose own stated validation gate fails non-deterministically.\n\nI did not edit Cargo.toml, Cargo.lock, README.md, src/*, or any file; I created no commits, tags, or pushes. The crate remains at 0.2.1 / HEAD 29d46f9.

## Full Agent Output

I have made **zero changes** — no edits, no commits, no tag, no push. The tree is exactly as I found it, which is correct per the "surface and stop" protocol.

I'm halting per the PRP's explicit failure protocol, because **two independent input preconditions are unmet** and the PRP forbids me from fixing either.

```json
{
  "result": "issue",
  "message": "HALTED at the pre-flight gate (Task 1) per the PRP's failure protocol ('surface it and stop'). I made NO changes — Cargo.toml is still 0.2.1, no commit, no tag, no push. Two independent input-precondition failures block this release chore:\n\n(1) DIRTY TREE — T3.S2's work is uncommitted. `git status` shows `M README.md` and `M src/lib.rs`. The README.md diff is exactly T3.S2's Edit H (the Programmatic-Usage example rewritten to `match` on `CommandResponse`), and src/lib.rs is a 3-line comment update in the tests module. The PRP's coordination directive is explicit: 'If git status shows README.md modified (uncommitted T3.S2 work), coordinate: T3.S2 must commit BEFORE the T4.S1 bump commit so the tag points at a tree with the README already updated.' These are NOT my files to commit (ownership boundary: README is T3.S2's; the bump commit must be version-only). The untracked `plan/001_b92a9b2b603f/P1M4T1S1/` is unrelated crate-local planning.\n\n(2) FLAKY PRE-FLIGHT TEST GATE — `cargo test --lib` (the PRP's literal gate) fails intermittently with a heap-corruption abort. Reproduction: observed 1 SIGABRT across ~24 default-parallel runs; the abort is `corrupted size vs. prev_size while consolidating` (signal 6), firing AFTER all 57 test cases print `ok`, during test-binary teardown. With `--test-threads=1` the suite is deterministic green (57 passed; 0 failed every time). Root cause (T1–T3 code, not this chore): the global `static DEVICE_CACHE: LazyLock<Mutex<Option<DeviceCache>>>` in src/core.rs:571 holds an `HidApi` + `Vec<HidDevice>`; the Mutex guards runtime access but NOT process-teardown ordering, so when multiple parallel test threads' lingering state interacts with hidapi-2.4.1's C-level `hid_exit`/handle-close during LazyLock Drop, glibc's tcache checker intermittently flags the resulting double/out-of-order free. The PRP forbids me from fixing this ('the bug is in T1-T3 CODE, not this release chore — do NOT fix it here; surface it and stop').\n\nREQUIRED before this chore can run:\n  • T3.S2 must commit its README.md + src/lib.rs changes (so the tree is clean and the bump commit can be version-only, per the v0.2.1 convention).\n  • Decision needed on the hidapi-teardown flake: either (a) make the crate's test teardown thread-safe (join/drop device handles explicitly before process exit, or gate HID-touching tests behind a serial mutex), or (b) formally accept the flake and change the PRP's gate to `cargo test --lib -- --test-threads=1`. Tagging flaky code as-is would ship a release whose own stated validation gate fails non-deterministically.\n\nI did not edit Cargo.toml, Cargo.lock, README.md, src/*, or any file; I created no commits, tags, or pushes. The crate remains at 0.2.1 / HEAD 29d46f9."
}
```

## Instructions

Review this feedback and create a revised PRP that addresses the issue.
If the issue is fundamentally impossible to resolve, output `"result": "fail"` with an explanation.
