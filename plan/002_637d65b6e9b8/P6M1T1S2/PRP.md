# PRP — P6.M1.T1.S2: Update `docs/qmk-integration.md` with host-rules migration guide

> **Repo under change:** the **qmkonnect** desktop app at
> `/home/dustin/projects/qmkonnect`. This is a **documentation-only** task
> (item CONTRACT: "DOCS: This IS the documentation subtask"). It edits **ONE**
> Markdown file — `docs/qmk-integration.md` (192 lines / 6.6 KB) — by adding a
> new H2 section (the **host-rules migration guide**) covering the five
> contract points: (1) the three-repo architecture, (2) `DEFINE_HOST_CALLBACKS`
> (one-time firmware change), (3) migration from `DEFINE_SERIAL_LAYERS`/
> `DEFINE_SERIAL_COMMANDS` to `rules.toml` (incremental, optional), (4) the
> stack-vs-replace coexistence model, and (5) a cross-link to
> `docs/configuration.md` for the full `rules.toml` schema + CLI flags.
>
> **File touched (1):** `docs/qmk-integration.md`. **Nothing else** — no Rust,
> no Cargo, no other docs, no `llms_full.txt` (regenerated by P6.M1.T1.S4).
>
> **This is NOT a code task.** P1–P5 are now **Complete/landed** (verified:
> `src/core/rules.rs` + `src/core/pattern.rs` present; `rules.rs` structs match
> the spec). Its inputs are the locked **spec** (`spec/HOST_RULES.md`
> §2/§4/§6/§10), the **canonical firmware README** at
> `/home/dustin/projects/qmk-notifier/README.md` (the tested
> `DEFINE_HOST_CALLBACKS` example), and the existing `docs/qmk-integration.md`.
> The only not-yet-landed dependency is the *sibling doc* S1
> (`docs/configuration.md`, still implementing) — treat its PRP as a hard
> **contract** (it links to `/qmk-integration` for this migration guide). Do
> NOT gate this doc on S1 landing; do NOT duplicate S1's schema content.

---

## Goal

**Feature Goal**: Make `docs/qmk-integration.md` the user-facing **migration
guide** for host-side window rules: a reader who has already integrated
`qmk-notifier` and written `DEFINE_SERIAL_LAYERS`/`DEFINE_SERIAL_COMMANDS`
rules should, after reading the new section, know (a) that host rules are an
opt-in overlay their board rules keep working alongside, (b) the three
repositories involved and what each does, (c) the one-time firmware change
(`DEFINE_HOST_CALLBACKS`) and how to write it, (d) the 4-step workflow for
moving an existing firmware rule into `rules.toml` (and *why* to remove it
from the firmware macro), and (e) how host rules coexist with board rules
(stack vs. replace, per window) — with a cross-link to `docs/configuration.md`
for the complete `rules.toml` schema and CLI flags.

**Deliverable** (`docs/qmk-integration.md`, edited):
1. **One new H2 section** — `## Host-Side Rules (Optional): Moving Window Rules to the Desktop` — inserted after the `## About RAW_USAGE_PAGE / RAW_USAGE_ID` section and before `## Testing Your Integration`, with:
   - an intro blockquote (opt-in; `proto_ver == 2` required; legacy/disconnected ⇒ silent string-only fallback, board rules unchanged),
   - a **three-repositories** subsection (table: qmkonnect / qmk_notifier / qmk-notifier, with the hyphen-vs-underscore naming hazard),
   - a **`DEFINE_HOST_CALLBACKS`** subsection (the named-registry concept + the canonical `mute` code example),
   - a **Migration** subsection (the 4 steps from `HOST_RULES.md` §10, in user terms, with the "why remove from the firmware macro" rationale + a single illustrative `[[callback_rules]]` snippet + a link to configuration.md for the full schema),
   - a **Stack vs. replace** subsection (plain-prose coexistence model + link to configuration.md for the `disable_firmware_config` field detail).
2. **Cross-links** to `{{ site.baseurl }}/configuration` (schema + CLI flags), and optionally `/examples` and `/troubleshooting`.

**Success Definition**:
- The new section renders cleanly under Jekyll (kramdown + rouge; ```c / ```toml fences balanced; no malformed `{{ site.baseurl }}` links).
- The `DEFINE_HOST_CALLBACKS` example matches the canonical firmware form (`{ name, on_enable, on_disable }`; `on_disable` may be `NULL`; `id` = array index) byte-for-byte with `/home/dustin/projects/qmk-notifier/README.md` (the tested `mute` example).
- The 4 migration steps faithfully adapt `HOST_RULES.md` §10, including the accurate "why remove" rationale (layers ⇒ same layer driven by both trackers = harmless but confusing; callbacks ⇒ additive = same `on_enable` fires twice).
- The stack-vs-replace explanation is correct: two planes (board / host) that touch only at `clear_board` + `SET_OS`; per-window host choice; stack = string-first then context, replace = context-only; a window is replace iff *every* matched rule is disabling (or board has no rules).
- The full `rules.toml` schema field table, the §9 annotated example, and the CLI-flag reference are **NOT** duplicated here — they live in configuration.md (S1); this page **links** to them.
- `git diff --stat` = `docs/qmk-integration.md` ONLY.

## User Persona (if applicable)

**Target User**: a power user who has already integrated the `qmk-notifier`
firmware module and written `DEFINE_SERIAL_LAYERS`/`DEFINE_SERIAL_COMMANDS`
rules, and now wants to move some/all of those rules to the host so they can
edit them **without reflashing**.

**Use Case**: "I have a working keyboard that switches layers per app via
firmware rules. I want to tweak those rules (or add new ones) from my desktop
without rebuilding and reflashing firmware every time. I open the QMK
Integration Guide, find the host-rules section, learn I need to add
`DEFINE_HOST_CALLBACKS` (one reflash), then move rules into `rules.toml` and
iterate with the tray 'Reload rules' — no more reflashing."

**User Journey**: read the intro blockquote (sets capability expectations) →
skim the three-repo table (understand the architecture) → copy the
`DEFINE_HOST_CALLBACKS` example, adapt to their callbacks, reflash once →
follow the 4 migration steps (add a `rules.toml` entry, remove the matching
firmware-macro row) → read the stack-vs-replace note to understand whether
their board rules still run → click over to configuration.md for the full
schema and CLI flags.

**Pain Points Addressed**: today `docs/qmk-integration.md` covers *only* the
legacy firmware-side setup (the 5 steps + `DEFINE_*` macros). A user who
discovers host rules (via the tray "Reload rules", a `rules.toml` file, or the
configuration.md schema added by S1) has no in-this-page migration guide tying
the new `DEFINE_HOST_CALLBACKS` firmware macro to their existing
`DEFINE_SERIAL_*` rules — and no explanation of how the two coexist.

## Why

- **Item CONTRACT point 3 (LOGIC)** names exactly the five things to document:
  three-repo architecture, `DEFINE_HOST_CALLBACKS`, migration from
  `DEFINE_SERIAL_*`, stack-vs-replace, and a reference to configuration.md.
- **`HOST_RULES.md` §10** is the canonical migration procedure; §2 is the
  three-repo table; §4 is the coexistence model; §6 is the
  `DEFINE_HOST_CALLBACKS` registry spec. The QMK Integration page is the
  firmware-side home for all of it.
- **Complements** P6.M1.T1.S1 (configuration.md owns the `rules.toml`
  **reference** — field table from `src/core/rules.rs`, §9 example, CLI flags,
  file location); this task owns the **migration/firmware** layer and links out
  to S1 for the schema. S1's configuration.md links back here for "the firmware
  side (including the `DEFINE_HOST_CALLBACKS` registry and migrating `DEFINE_*`
  rules)."
- The firmware repo's own README (`/home/dustin/projects/qmk-notifier/README.md`,
  "Host-Side Rules & Typed Commands") is the authoritative source for the
  `DEFINE_HOST_CALLBACKS` example and the coexistence prose; this page mirrors
  it from the **QMKonnect (desktop) perspective**.

## What

**One additive edit** to `docs/qmk-integration.md`: insert a new H2 section
plus subsections (full content in "All Needed Context" → "Implementation
Tasks"). The existing sections — Overview, Integration Steps (1–5),
`## About RAW_USAGE_PAGE / RAW_USAGE_ID`, `## Testing Your Integration`,
`## Common Issues`, `## Next Steps` — stay **verbatim** (no edits, no
rewording). The only change to existing content is optional: a single trailing
pointer line in Step 4 ("Want to move these rules to the host? See … below.")
and/or a bullet in Next Steps. Everything else is inserted, not modified.

### Success Criteria
- [ ] New `## Host-Side Rules (Optional): Moving Window Rules to the Desktop`
      H2 section exists, inserted **after** `## About RAW_USAGE_PAGE / RAW_USAGE_ID`
      and **before** `## Testing Your Integration`.
- [ ] Intro blockquote states host rules are **opt-in**, require `proto_ver == 2`
      firmware (the `APPLY_HOST_CONTEXT` feature), and that legacy firmware or a
      disconnected keyboard ⇒ silent string-only fallback with board rules
      unchanged.
- [ ] A **three-repositories** subsection (or table) names `qmkonnect`
      (desktop daemon, this repo), `qmk_notifier` **(underscore)** (Rust
      transport crate), and `qmk-notifier` **(hyphen)** (C firmware module),
      with roles; includes the **hyphen-vs-underscore naming-hazard** note.
- [ ] A **`DEFINE_HOST_CALLBACKS`** subsection shows the canonical example
      (`{ "mute", &mute_on, &mute_off }`), states each row is
      `{ name, on_enable, on_disable }` (`on_disable` may be `NULL`), the `id`
      is the **array index** (stable per build; re-queried by name on reconnect),
      omitting the macro ⇒ byte-for-byte today (weak accessors, `count == 0`),
      and that it needs **no `rules.mk` change** (define in `keymap.c`).
- [ ] A **Migration** subsection presents the 4 steps (expose callbacks → move
      a layer rule → move a callback rule → iterate without reflashing), with
      the accurate "why remove from the firmware macro" rationale
      (layers: same layer driven by both trackers = harmless but confusing;
      callbacks: additive = same `on_enable` fires twice), a single
      illustrative `[[callback_rules]]` snippet, and a link to
      `{{ site.baseurl }}/configuration` for the **full schema + CLI flags**.
- [ ] A **Stack vs. replace** subsection explains the two coexistence modes in
      plain prose (stack = string-first then host context; replace =
      context-only with `clear_board`; a window is replace iff *every* matched
      rule is disabling or the board has no rules), and links to
      `{{ site.baseurl }}/configuration` for the `disable_firmware_config` field detail.
- [ ] ```c / ```toml fences are balanced (even count of ``` lines file-wide).
- [ ] All new internal links use `{{ site.baseurl }}/slug` (no `.md`, no `https`,
      no leading slash before `{{`).
- [ ] The full `rules.toml` schema field table and the §9 annotated example are
      **absent** from this page (owned by S1/configuration.md) — only cross-links.
- [ ] `git diff --stat` = `docs/qmk-integration.md` ONLY.

## All Needed Context

### Context Completeness Check

_Pass_: an agent with no prior knowledge of this codebase can implement this
using only this PRP + `research/notes.md`, because: (a) the exact current
H2/H3 skeleton of `docs/qmk-integration.md` (line-accurate, incl. the insertion
boundary `## Testing Your Integration`) is in research §1.1/§1.3; (b) the
verbatim 4-step migration procedure to adapt is in research §0.1 (copied from
`HOST_RULES.md` §10); (c) the canonical `DEFINE_HOST_CALLBACKS` example
(byte-for-byte from the firmware README, verified unchanged) is in research
§0.2; (d) the three-repo table (roles + URLs + naming hazard) is in research
§0.3; (e) the stack-vs-replace model (two planes, `clear_board`, per-window AND
decision) is in research §0.4; (f) the landed `rules.toml` schema (verified
against `src/core/rules.rs`) is in research §0.6 (for cross-link accuracy);
(g) the cross-link + fence conventions are in research §1.2; (h) the boundary
with S1/S3/S4 (do NOT duplicate the schema, recipes, or regenerate
`llms_full.txt`) is in research §2; (i) design decisions D1–D6 and gotchas
G1–G7 are in research §3/§4; (j) the validation commands are in research §5.
No code/build dependency.

### Documentation & References

```yaml
# MUST READ — THIS task's full contract (verbatim §10 migration, DEFINE_HOST_CALLBACKS
# example, three-repo table, stack/replace model, current file skeleton, boundary,
# validation).
- file: plan/002_637d65b6e9b8/P6M1T1S2/research/notes.md
  why: "§0.1 = the verbatim §10 4-step migration (adapt for users). §0.2 = the
        canonical DEFINE_HOST_CALLBACKS example (copy the `mute` form byte-for-byte).
        §0.3 = the three-repo table + URLs + naming hazard. §0.4 = the stack/replace
        two-plane model + clear_board + per-window decision. §0.6 = the LANDED rules.rs
        schema (for the cross-link's accuracy; the migration guide LINKS OUT to it, not
        reproduce). §1 = line-accurate current qmk-integration.md skeleton + insertion
        point + cross-link conventions. §2 = boundary (S1=configuration schema/CLI,
        S3=recipes, S4=llms_full.txt regen — do NOT duplicate). §3/§4 = D1-D6 design +
        G1-G7 gotchas. §5 = validation."

# MUST READ — the canonical migration procedure (single source of truth for the steps).
- file: spec/HOST_RULES.md
  why: "§10 ('Migration from DEFINE_*') = the 4 steps. §2 = the three-repo table.
        §4 = the stack/replace coexistence model (the ASCII flow + the precise
        semantics). §6 = the DEFINE_HOST_CALLBACKS registry spec (struct + weak
        accessors + id=index). §3 B1/B2/C7/C10/C11 = the locked design (coexistence,
        registry, no-match-clear, per-rule disable_firmware_config, host layers >= 224)."
  section: "§10 (migration), §2 (repos), §4 (coexistence), §6 (DEFINE_HOST_CALLBACKS)"

# MUST READ — the authoritative DEFINE_HOST_CALLBACKS example + coexistence prose.
- file: /home/dustin/projects/qmk-notifier/README.md
  why: "The firmware repo's own user-facing README. The 'Host-Side Rules & Typed
        Commands' section (≈L291+) has the canonical, tested DEFINE_HOST_CALLBACKS
        example (the `mute` callback, ≈L320-338), the 'How it works' two-plane
        explanation, the 'Stack vs replace per window' prose, the 'Backward
        compatibility' guarantee, and the 'Companion Projects' table with exact
        repo URLs. This page MIRRORS that content from the QMKonnect/desktop
        perspective; copy the example byte-for-byte."
  section: "'Host-Side Rules & Typed Commands' (example, how-it-works, stack/replace,
            backward compat, companion projects)"

# MUST READ — the file THIS task edits (the ONLY file).
- file: docs/qmk-integration.md
  why: "192-line Jekyll page. The new H2 section is INSERTED after the
        '## About RAW_USAGE_PAGE / RAW_USAGE_ID' section (≈L121-129) and BEFORE
        '## Testing Your Integration' (L130). The existing '### Step 4: Define
        layer and command rules' (L83) shows the DEFINE_SERIAL_* example that
        the migration section references (the 'before' state). Cross-links use
        '{{ site.baseurl }}/slug'; code fences are labeled (```c / ```make / ```bash)."
  pattern: "front matter (layout: default, title: QMK Integration, permalink:
            /qmk-integration/); ```c fences; `[text]({{ site.baseurl }}/page)` links;
            blockquote intros (`> ...`)."
  gotcha: "do NOT edit the existing Overview / Steps / RAW_USAGE_PAGE / Testing /
           Common Issues / Next Steps sections (keep verbatim). The only optional
           touch to existing content is a single trailing pointer line in Step 4 +
           a Next Steps bullet. do NOT duplicate the rules.toml schema field table
           or the §9 example (configuration.md/S1 owns those). do NOT regenerate
           llms_full.txt (S4)."

# MUST READ — the sibling task's contract (S1 owns the schema reference; LINK to it).
- file: plan/002_637d65b6e9b8/P6M1T1S1/PRP.md
  why: "S1's configuration.md owns: the rules.toml schema field table (from
        src/core/rules.rs), the verbatim §9 annotated example, the per-OS file-location
        table, the 3 CLI flags (--list-callbacks / --validate-rules / --rules-path),
        the -c seeding note, and the stack-vs-replace field-level semantics. S1 LINKS
        OUT to /qmk-integration for 'the firmware side (DEFINE_HOST_CALLBACKS +
        migration)' (PRP lines 360-361), and explicitly states 'the migration GUIDE
        is S2 (qmk-integration.md)' (lines 727-728). This task (S2) is the TARGET of
        that link — it must contain that content and LINK BACK to /configuration for
        the schema. Treat S1's PRP as a contract: assume it lands as specified; do
        not duplicate its schema content."
  section: "Goal + What (the configuration.md section shape) + Cross-links (S1 → /qmk-integration)"

# REFERENCE — the LANDED schema (P3.M1, verified present) — for cross-link accuracy.
- file: src/core/rules.rs
  why: "The LANDED rules.toml data model (P3.M1 Complete). RuleSet/HostDefaults/
        LayerRule/CallbackRule + Pattern enum. The migration guide's ONE illustrative
        [[callback_rules]] snippet (match = 2-element array, enable = [names]) and
        its cross-link to /configuration must be consistent with these structs. This
        task does NOT reproduce the field table — S1 does — but the snippet it shows
        must be valid against this schema."
  pattern: "read the 4 struct definitions: TOML key is `match` (Rust field `pattern`);
            `layer: u8` REQUIRED (host range >= 224); disable_firmware_config is `bool`
            in HostDefaults and `Option<bool>` per-rule; Pattern is Single(String) |
            Parts(String, String) (`\"foo\"` | `[\"cls\",\"ttl\"]`)."
  gotcha: "the docs use the TOML KEY `match` (not the Rust field name `pattern`);
           the TOML table-arrays are [[layer_rules]] / [[callback_rules]]."

# REFERENCE — the firmware macro API (DEFINE_SERIAL_COMMANDS shape, callback_t, WT()).
- file: spec/FIRMWARE.md
  why: "§3 (notifier.h macros) defines callback_t, command_map_t {pattern, on_enable,
        on_disable, case_sensitive}, layer_map_t, DEFINE_SERIAL_COMMANDS/
        DEFINE_SERIAL_LAYERS, and WT(class,title). The migration maps the SAME
        on_enable/on_disable C functions into DEFINE_HOST_CALLBACKS rows by name.
        §2 Step 4 shows the DEFINE_SERIAL_* example this page already embeds."
  section: "§3 (macros & structs), §2 Step 4 (the rules example)"
  gotcha: "DEFINE_HOST_CALLBACKS rows are {name, on_enable, on_disable} — NOTE the
           field ORDER and that on_disable may be NULL. Do not confuse with
           command_map_t which is {pattern, on_enable, on_disable, case_sensitive}
           (pattern is NOT a field of the host-callback registry — the pattern moves
           to rules.toml)."
```

### Current Codebase tree (relevant subset)

```bash
docs/
  qmk-integration.md    # ← THIS TASK EDITS (the ONLY file): + one new H2 section
                        #   (intro + three-repos + DEFINE_HOST_CALLBACKS + migration +
                        #   stack/replace), + cross-links to /configuration (and optionally
                        #   /examples, /troubleshooting). Jekyll page (kramdown/rouge).
  configuration.md      # P6.M1.T1.S1 (rules.toml schema + CLI flags). UNCHANGED (linked OUT).
  examples.md           # P6.M1.T1.S3 (host-rules recipes). UNCHANGED (linked OUT).
  troubleshooting.md    # P6.M1.T1.S3. UNCHANGED (linked OUT).
  README.md             # repo README (also under /). P6.M1.T1.S4. UNCHANGED.
  llms_full.txt         # GENERATED concatenation of docs/*.md. P6.M1.T1.S4 regenerates
                        #   it AFTER all doc edits land. NOT touched by this task.
  _config.yml           # Jekyll: baseurl "/qmkonnect", markdown kramdown, highlighter rouge.
src/core/
  rules.rs              # P3.M1 LANDED (Complete) — RuleSet/HostDefaults/LayerRule/
                        #   CallbackRule + Pattern + parse_rules + evaluate. READ-ONLY
                        #   (the migration guide's snippet + cross-link must be consistent
                        #   with this schema; it is NOT reproduced here).
  pattern.rs            # P2.M1 LANDED (Complete) — full-parity matcher. READ-ONLY.
spec/
  HOST_RULES.md         # §10 migration + §2 repos + §4 coexistence + §6 registry. READ-ONLY.
  FIRMWARE.md           # §3 notifier.h macros (callback_t, DEFINE_SERIAL_*, WT). READ-ONLY.
# (external, READ-ONLY) the authoritative firmware README:
/home/dustin/projects/qmk-notifier/README.md   # "Host-Side Rules & Typed Commands" — the
                        # canonical DEFINE_HOST_CALLBACKS example + coexistence prose.
```

### Desired Codebase tree with files to be changed

```bash
docs/qmk-integration.md    # MODIFIED: + "## Host-Side Rules (Optional): Moving Window Rules
                           #   to the Desktop" H2 section (intro blockquote + three-repos
                           #   table + DEFINE_HOST_CALLBACKS subsection w/ canonical example +
                           #   4-step migration subsection + stack-vs-replace subsection),
                           #   + cross-links to /configuration (schema + CLI flags). Optional:
                           #   one trailing pointer line in Step 4 + a Next Steps bullet.
# EVERYTHING ELSE UNCHANGED. No Rust, no Cargo.toml, no other docs/, no llms_full.txt, no spec.
```

### Known Gotchas of our codebase & Library Quirks

```markdown
<!-- CRITICAL (G1 — NOT a code task): P1-P5 are now landed (Complete). There are NO
     code blockers. The only not-yet-landed dependency is the sibling DOC S1
     (docs/configuration.md, still implementing) — treat its PRP as a contract and
     LINK to /configuration; do not gate this doc on S1 landing. -->

<!-- CRITICAL (G2 — do NOT duplicate the schema). The full rules.toml field table,
     the verbatim §9 annotated example, the per-OS file-location table, and the CLI
     flags are OWNED by S1 (configuration.md). LINK to {{ site.baseurl }}/configuration.
     A SINGLE illustrative [[callback_rules]] snippet in the migration subsection is
     fine; the full table/example is not. -->

<!-- CRITICAL (G3 — do NOT touch llms_full.txt). It is a GENERATED concatenation;
     P6.M1.T1.S4 regenerates it AFTER all doc edits land. -->

<!-- GOTCHA (G4 — external repo links). qmk-notifier (HYPHEN) = firmware C module;
     qmk_notifier (UNDERSCORE) = Rust transport crate. The existing page already links
     the hyphen form for the firmware README. If you link the crate, use the underscore
     URL (https://github.com/dabstractor/qmk_notifier). Include the naming-hazard note. -->

<!-- GOTCHA (G5 — DEFINE_HOST_CALLBACKS example must match the canonical firmware form).
     Each row { name, on_enable, on_disable }; on_disable may be NULL; id = array index
     (stable per build, re-queried by name on reconnect). Use the `mute` example from
     the firmware README — do NOT invent a different struct/shape. The pattern is NOT a
     field of the host-callback registry (the pattern moves to rules.toml). -->

<!-- GOTCHA (G6 — accurate migration rationale). Layer rules: keeping in both = same
     layer driven by two trackers (harmless but confusing). Callback rules: callbacks
     are additive = same on_enable fires twice. This is the REASON to remove from the
     firmware macro. Do NOT say 'the board rule stops working' — it keeps working and
     conflicts; do NOT say removal is required for correctness (it's to avoid confusion
     / double-firing). -->

<!-- GOTCHA (G7 — Jekyll Markdown). Use labeled fences (```c / ```toml / ```bash — rouge).
     Internal links: `{{ site.baseurl }}/slug` (NOT https, NOT .md). The page permalink
     is /qmk-integration/ (already in front matter — don't touch it). Keep the new H2
     heading free of backticks/parens so its kramdown anchor is predictable (S1 links
     to /qmk-integration, the page; no same-page anchors are required by the contract). -->
```

## Implementation Blueprint

### Document structure (the deliverable)

The new content. Exact wording is in the Implementation Tasks below; this is
the shape. Copy the `DEFINE_HOST_CALLBACKS` example **verbatim** from
`research/notes.md` §0.2 (it is byte-for-byte the firmware README's tested
form). Adapt the §10 migration steps into user terms (research §0.1) — do not
paraphrase the *rationale* inaccurately (see G6).

#### The new `## Host-Side Rules` section

```markdown
## Host-Side Rules (Optional): Moving Window Rules to the Desktop

So far this guide covers the **board-side** rules — `DEFINE_SERIAL_LAYERS` and
`DEFINE_SERIAL_COMMANDS`, baked into your firmware. QMKonnect can also drive
layer and callback decisions **from the host**, in an editable `rules.toml`
file, so you can change them **without reflashing**. Host rules are an opt-in
overlay: your existing board rules keep working, and host rules either stack on
top of them or take over per window (see [Stack vs. replace](#stack-vs-replace)
below).

> **Firmware prerequisite.** Host rules require firmware that advertises the
> typed-command capability (`proto_ver == 2` with the `APPLY_HOST_CONTEXT`
> feature). With legacy firmware — or while the keyboard is disconnected —
> QMKonnect silently falls back to today's string-only behavior, and your
> board's existing `DEFINE_*` rules keep working exactly as before. Nothing
> breaks if you don't opt in.

### The three repositories

Host rules span three companion repos. You only ever edit two of them (your
firmware keymap, and `rules.toml` on the host):

| Repo | What it is | Its job for host rules |
| --- | --- | --- |
| [`qmkonnect`](https://github.com/dabstractor/qmkonnect) | The desktop daemon (this project) | Detects the window; owns `rules.toml`, the host matcher, the capability handshake, and the send sequencing |
| [`qmk_notifier`](https://github.com/dabstractor/qmk_notifier) *(underscore)* | The Rust transport crate QMKonnect links | Frames the typed commands over Raw HID and parses the replies |
| [`qmk-notifier`](https://github.com/dabstractor/qmk-notifier) *(hyphen)* | The C firmware module in your keymap | Receives typed commands, tracks a separate host layer/callback set, and exposes your callbacks by name |

> **Naming hazard:** `qmk-notifier` (hyphen) is the firmware C module;
> `qmk_notifier` (underscore) is the Rust transport crate. They talk over the
> fixed Raw HID wire protocol described in the
> [qmk-notifier README](https://github.com/dabstractor/qmk-notifier).

### Expose callbacks by name: `DEFINE_HOST_CALLBACKS` (one-time firmware change)

Host rules reference your callbacks **by name** (a string), not by pointer. So
that the host can look them up, you declare a small named registry in your
firmware. This is a **one-time** change — add it once, reflash once, and you
never touch it again when iterating on rules.

Each row is `{ name, on_enable, on_disable }`, where `on_disable` may be `NULL`.
The callback's `id` is its **array index** (stable per build); QMKonnect queries
the names at every (re)connect, so renumbering across flashes is harmless. It
needs **no `rules.mk` change** — define it anywhere `#include`-d from
`keymap.c`, just like `DEFINE_SERIAL_COMMANDS`:

```c
static void mute_on(void)  { /* unmute / show mute OSD */ }
static void mute_off(void) { /* restore */ }

DEFINE_HOST_CALLBACKS({
    { "mute", &mute_on, &mute_off },
});
```

These are the **same C functions** you already pass to `DEFINE_SERIAL_COMMANDS`
— you're just listing them by name so the host can address them. Omit the macro
entirely and the firmware provides empty defaults (`callback_count == 0`), the
feature stays off, and your keymap behaves byte-for-byte as it does today.

### Migration: from `DEFINE_*` to `rules.toml`

Migration is **incremental and optional** — move one rule at a time, or none at
all. Board rules keep working throughout.

1. **Expose your callbacks by name** — add `DEFINE_HOST_CALLBACKS({ … })`
   (above) listing the functions you already use in `DEFINE_SERIAL_COMMANDS`.
   Reflash **once**. (This is the only step that ever requires a reflash.)
2. **Move a layer rule to the host** — add a `[[layer_rules]]` entry to
   `rules.toml`, then **remove** the matching row from `DEFINE_SERIAL_LAYERS`.
   (Keeping it in both isn't harmful, but it means the same layer is driven by
   two trackers at once, which is confusing.) No reflash needed for this or any
   later edit.
3. **Move a callback rule to the host** — add a `[[callback_rules]]` entry,
   then **remove** the matching row from `DEFINE_SERIAL_COMMANDS`. Here removal
   matters: callbacks are additive, so if a rule stays in both, the same
   `on_enable` would fire twice.
4. **Iterate without reflashing** — edit `rules.toml` and click the tray's
   **Reload rules** (or restart QMKonnect). Every future rule change is a host
   edit, no firmware rebuild.

For example, the firmware callback rule
`{ WT("steam_app*", "*"), &disable_vim }` (already listed in
`DEFINE_HOST_CALLBACKS` as `{ "disable_vim", &disable_vim, NULL }`) becomes a
host rule:

```toml
[[callback_rules]]
match = ["steam_app*", "*"]        # [class, title]  == WT(class, title)
enable = ["disable_vim"]
```

The full `rules.toml` schema (every field, the layer range, the `match`
string-vs-array forms), the per-OS file location, and the
`--validate-rules` / `--list-callbacks` / `--rules-path` CLI flags are
documented in the [Configuration Guide]({{ site.baseurl }}/configuration).

### Stack vs. replace

The firmware keeps **two independent state planes**: your **board** layer and
callbacks (driven by the legacy string + `DEFINE_*`, exactly as today) and a
separate **host** layer and callback set (driven by `rules.toml`). Host layers
are reserved **≥ 224** so they always resolve above your board layers; `255`
clears the host layer. The two planes touch only at two seams, and QMKonnect
picks which one per window:

- **Stack** (the default): QMKonnect sends the window **string first** — so your
  board runs its own `DEFINE_*` rules as usual — and then applies the host
  layer **on top** and syncs the host callbacks. Board callbacks fire first,
  then host callbacks.
- **Replace**: for a window where you want the host to fully take over,
  QMKonnect sends **only** the host context (no string, so the board can't
  match). The firmware clears its board layer/command for that window and
  applies only the host layer + callbacks. Board rules re-engage normally on
  the host's next string send.

Which mode a window uses is decided by each rule's `disable_firmware_config`
flag in `rules.toml`: a window is *replace* only when **every** rule that
matches it is disabling (or the board has no rules of its own); if even one
matched rule is non-disabling, the window *stacks*. See the
[Configuration Guide]({{ site.baseurl }}/configuration) for the field-level
detail. If no window matches, the host layer is cleared and all host callbacks
are disabled (there is no "keep" option).
```

> *(The heading anchor `#stack-vs-replace` resolves under kramdown's
> auto-anchor slugification. The single `[Stack vs. replace](#stack-vs-replace)`
> link in the intro block resolves to it. If you change the heading text,
> update both. Render once to confirm the real anchor if unsure.)*

#### Optional touches to existing sections (low-risk, improves discoverability)

Two **optional** one-liners, both purely additive (no edits to existing prose):

1. **In `### Step 4: Define layer and command rules`** (L83), append a single
   trailing paragraph after the existing `### Step 5: Build and flash` *or* at
   the end of Step 4's prose:
   `> Want to change these rules later without reflashing? See [Host-Side Rules](#host-side-rules-optional-moving-window-rules-to-the-desktop) below.`
   (Use the real heading anchor.)
2. **In `## Next Steps`** (L188), add a bullet:
   `- [Move rules to the host (optional)]({{ site.baseurl }}/qmk-integration#host-side-rules-optional-moving-window-rules-to-the-desktop)`

Both are optional; the new H2 section is discoverable on its own. If the
heading text differs from the recommended `## Host-Side Rules (Optional):
Moving Window Rules to the Desktop`, recompute the anchor and keep the two
links consistent.

### Implementation Tasks (ordered by dependencies)

```yaml
Task 1: READ the current file + research/notes.md + the two spec sources
  - READ docs/qmk-integration.md in full (192 lines) to confirm the section
    skeleton and locate the insertion boundary: the END of the
    "## About RAW_USAGE_PAGE / RAW_USAGE_ID" section (≈L129) / the line
    "## Testing Your Integration" (L130).
  - READ plan/002_637d65b6e9b8/P6M1T1S2/research/notes.md §0.1 (§10 migration),
    §0.2 (DEFINE_HOST_CALLBACKS example), §0.3 (three-repo table), §0.4 (stack/replace).
  - READ spec/HOST_RULES.md §10 + §2 + §4 + §6 to confirm the migration steps,
    repo table, and coexistence model match notes.md.
  - READ /home/dustin/projects/qmk-notifier/README.md ("Host-Side Rules & Typed
    Commands", ≈L291-440) to confirm the DEFINE_HOST_CALLBACKS example (the
    `mute` callback) and the stack/replace prose are byte-identical to notes §0.2/§0.4.
  - (Optional sanity) grep src/core/rules.rs to confirm the schema the cross-link
    points at matches notes §0.6 (TOML key `match`; layer u8; Pattern Single/Parts).
  - GOTCHA G1: there are no code blockers (P1-P5 Complete); do not gate on S1 landing.

Task 2: INSERT the new "## Host-Side Rules (Optional): Moving Window Rules to the
        Desktop" H2 section (the blueprint block above)
  - INSERT the entire new H2 section (intro + three-repos table + DEFINE_HOST_CALLBACKS
    subsection + Migration subsection + Stack-vs-replace subsection) AFTER the
    "## About RAW_USAGE_PAGE / RAW_USAGE_ID" section and BEFORE "## Testing Your
    Integration".
  - COPY the DEFINE_HOST_CALLBACKS `mute` example BYTE-FOR-BYTE from research/notes.md
    §0.2 (or the firmware README) into a ```c fence. Do NOT invent a different shape.
  - ADAPT the 4 migration steps from HOST_RULES.md §10 (notes §0.1) into user prose,
    keeping the accurate "why remove" rationale (layers: two trackers = confusing;
    callbacks: additive = double-fire). Include the single `[[callback_rules]]`
    ```toml snippet that maps the existing `{ WT("steam_app*", "*"), &disable_vim }`
    firmware rule to a host rule.
  - WRITE the three-repos table from notes §0.3 (qmkonnect / qmk_notifier underscore /
    qmk-notifier hyphen) with the real repo URLs + the naming-hazard blockquote.
  - WRITE the Stack-vs-replace subsection from notes §0.4 (two planes; stack =
    string-first then host context; replace = context-only; per-window AND decision;
    host layers >= 224; no-match clears).
  - ADD cross-links via {{ site.baseurl }}: /configuration (full schema + CLI flags).
    Optionally /examples and /troubleshooting.
  - KEEP all existing sections (Overview, Steps 1-5, RAW_USAGE_PAGE, Testing, Common
    Issues, Next Steps) VERBATIM — insert only.
  - GOTCHA G2: do NOT paste the full rules.toml field table or the §9 example.
  - GOTCHA G4: hyphen (firmware) vs underscore (crate) in repo links.
  - GOTCHA G5: DEFINE_HOST_CALLBACKS row = { name, on_enable, on_disable }.
  - GOTCHA G6: removal rationale is "avoid confusion / double-fire", not correctness.
  - VERIFY: grep -c 'DEFINE_HOST_CALLBACKS' (>=1); grep -c 'rules.toml' (>=3);
    grep 'site.baseurl.*/configuration' (link present); grep -ni 'stack\|replace'.

Task 3: (OPTIONAL) ADD discoverability one-liners in Step 4 + Next Steps
  - In "### Step 4: Define layer and command rules" OR just before "### Step 5",
    append a single pointer line to the new section (blueprint §"Optional touches").
  - In "## Next Steps", add a bullet linking to the new section anchor.
  - KEEP existing Step 4 / Next Steps prose verbatim — only append.
  - If the heading text differs from the recommended one, recompute the anchor and
    keep the two links consistent.
  - GOTCHA G7: labeled fences + {{ site.baseurl }}/slug links only.

Task 4: VALIDATE (Markdown — NO cargo)
  - RUN the §5 validation commands from research/notes.md:
      git diff --stat                 # expect docs/qmk-integration.md ONLY
      grep -c 'DEFINE_HOST_CALLBACKS' docs/qmk-integration.md   # >= 1
      grep -c 'rules.toml' docs/qmk-integration.md              # >= 3
      grep -n 'stack\|replace' docs/qmk-integration.md          # the coexistence subsection
      grep -n 'site.baseurl.*/configuration' docs/qmk-integration.md  # cross-link
      test $(( $(grep -c '^```' docs/qmk-integration.md) % 2 )) -eq 0 \
        && echo "fences balanced" || echo "UNBALANCED FENCES"
  - (Optional) Jekyll build if ruby/bundle present: cd docs && bundle exec jekyll build.
  - EYEBALL: confirm the DEFINE_HOST_CALLBACKS example matches the firmware README,
    the 4 migration steps match §10, and the stack/replace prose is correct.
```

### Implementation Patterns & Key Details

```markdown
<!-- PATTERN: the existing page's labeled code fences (```c for C at Step 3/4,
     ```make at Step 2, ```bash at Step 1/5). Match that — use ```c for the
     DEFINE_HOST_CALLBACKS example and ```toml for the rules.toml snippet. Never
     bare ``` for code (rouge won't highlight it). -->

<!-- PATTERN: internal links are `[text]({{ site.baseurl }}/slug)` — every existing
     link in the file uses this (e.g. {{ site.baseurl }}/configuration,
     {{ site.baseurl }}/examples, {{ site.baseurl }}/troubleshooting). The slug has
     NO trailing .md and NO leading / before the baseurl expansion. For same-page
     anchors use `(#heading-slug)` (kramdown auto-slugs headings). -->

<!-- PATTERN: blockquote intros (`> ...`) — the page opens with one and uses them
     for callouts (e.g. the RAW_USAGE_PAGE note). Use a blockquote for the firmware
     prerequisite + the naming hazard, matching the existing style. -->

<!-- KEY DETAIL: the `mute` DEFINE_HOST_CALLBACKS example is the firmware repo's
     OWN tested example (not invented). Copy it verbatim — it documents the exact
     row shape { name, on_enable, on_disable } a user must follow. -->

<!-- KEY DETAIL: the migration's single [[callback_rules]] snippet maps the
     firmware rule already shown in Step 4 (`{ WT("steam_app*", "*"), &disable_vim }`)
     to its host equivalent. This creates a concrete before/after the reader can
     follow — reuse the SAME app/callback names that already appear on the page
     (don't introduce unrelated examples). -->

<!-- KEY DETAIL: the heading anchor for "### Stack vs. replace" is #stack-vs-replace
     under kramdown (lowercase, spaces->hyphens, slashes dropped). The intro's
     [Stack vs. replace](#stack-vs-replace) link resolves to it. If you rename the
     heading, update the link. -->
```

### Integration Points

```yaml
DOCUMENT STRUCTURE: the new H2 is INSERTED between "## About RAW_USAGE_PAGE /
  RAW_USAGE_ID" and "## Testing Your Integration". No existing section is moved,
  removed, or reworded (only optional trailing pointer lines in Step 4 / Next Steps).

CROSS-LINKS (added — Jekyll {{ site.baseurl }}):
  - {{ site.baseurl }}/configuration   (full rules.toml schema + CLI flags — S1)
  - {{ site.baseurl }}/examples        (optional — host-rules recipes — S3)
  - {{ site.baseurl }}/troubleshooting (optional — legacy-fallback/validate recipes — S3)
  - same-page anchor #stack-vs-replace (from the intro block)
  - same-page anchor #host-side-rules-… (optional, from Step 4 / Next Steps pointers)
  - external: https://github.com/dabstractor/qmk-notifier  (firmware README — already linked)
  - external: https://github.com/dabstractor/qmk_notifier  (crate — new, if referenced)

INBOUND LINK (from S1): configuration.md (S1) links to {{ site.baseurl }}/qmk-integration
  for "the firmware side (DEFINE_HOST_CALLBACKS registry + migrating DEFINE_* rules)".
  The new heading must be present and discoverable so that link resolves to real content.

DOWNSTREAM CONSUMERS:
  - P6.M1.T1.S4 (README.md + llms_full.txt regen): llms_full.txt is a GENERATED
    concatenation of docs/*.md. This task's qmk-integration.md edits will be picked
    up when S4 regenerates it AFTER all doc edits land. Do NOT regenerate it here.

BUILD: none. Jekyll (docs/_config.yml: markdown kramdown, highlighter rouge,
  baseurl "/qmkonnect", remote_theme midnight) renders the page on GitHub Pages.
  An optional local `bundle exec jekyll build` validates Liquid/fence syntax but
  is not required.
```

## Validation Loop

### Level 1: Markdown consistency (Immediate Feedback)

```bash
cd /home/dustin/projects/qmkonnect

# Scope gate: ONLY docs/qmk-integration.md changed.
git diff --stat
# EXPECT: docs/qmk-integration.md ONLY.

# The new content landed (firmware change + migration + coexistence).
grep -c 'DEFINE_HOST_CALLBACKS' docs/qmk-integration.md   # >= 1
grep -c 'rules.toml' docs/qmk-integration.md              # >= 3
grep -ni 'stack\|replace' docs/qmk-integration.md         # the coexistence subsection
grep -n 'site.baseurl.*/configuration' docs/qmk-integration.md  # cross-link present
grep -n 'qmk_notifier\|qmk-notifier' docs/qmk-integration.md   # three-repo names + URLs

# Balanced code fences (even count of ``` lines).
test $(( $(grep -c '^```' docs/qmk-integration.md) % 2 )) -eq 0 \
  && echo "fences balanced" || echo "UNBALANCED FENCES — fix before committing"

# Internal link slugs are well-formed (no .md, no double slashes, no https for internal).
grep -oE '\{\{ site\.baseurl \}\}/[a-z-]+' docs/qmk-integration.md | sort -u
```

### Level 2: Content-fidelity cross-check (Component Validation)

```bash
cd /home/dustin/projects/qmkonnect

# The DEFINE_HOST_CALLBACKS example matches the canonical firmware form
# (row shape { name, on_enable, on_disable }; the `mute` example).
grep -A4 'DEFINE_HOST_CALLBACKS({' docs/qmk-integration.md
# EXPECT: the `{ "mute", &mute_on, &mute_off }` row, byte-for-byte with the firmware README.

# The 4 migration steps are present and in order.
grep -nE 'Move a layer rule|Move a callback rule|Expose.*callbacks by name|Iterate' docs/qmk-integration.md

# The accurate "why remove" rationale is stated (not a correctness claim).
grep -ni 'two trackers\|confusing\|additive\|fire twice\|twice' docs/qmk-integration.md

# The three-repo table is present (all three repo names).
grep -c 'qmkonnect' docs/qmk-integration.md          # >= several
grep -c 'qmk_notifier' docs/qmk-integration.md       # the crate (underscore) — >= 1
grep -c 'qmk-notifier' docs/qmk-integration.md       # the firmware (hyphen) — >= several

# Stack-vs-replace: per-window AND decision + host layers >= 224.
grep -ni 'every\|per window\|224' docs/qmk-integration.md

# Consistency with the LANDED schema (the snippet's match form + enable list are valid).
grep -A2 '\[\[callback_rules\]\]' docs/qmk-integration.md
```

### Level 3: Render check (System Validation — optional, highest confidence)

```bash
cd /home/dustin/projects/qmkonnect/docs

# If ruby + bundler are available, build the Jekyll site. Catches Liquid syntax
# errors, broken {{ site.baseurl }} expansions, and malformed fences that a grep
# can miss. (Not required for correctness — GitHub Pages renders it on push — but
# it's the strongest local signal.)
bundle exec jekyll build 2>&1 | grep -iE 'error|warning' || echo "jekyll build clean"

# Then open _site/qmk-integration/index.html in a browser and confirm:
#   - the new "Host-Side Rules (Optional)…" H2 appears between the RAW_USAGE_PAGE
#     section and "Testing Your Integration",
#   - the DEFINE_HOST_CALLBACKS C block is syntax-highlighted (rouge) and intact,
#   - the three-repo table renders,
#   - the cross-link to /configuration/ resolves,
#   - the same-page #stack-vs-replace anchor works,
#   - all existing sections (Overview, Steps 1-5, Testing, Common Issues, Next Steps)
#     are unchanged.
```

### Level 4: Boundary + scope gate (Domain-Specific)

```bash
cd /home/dustin/projects/qmkonnect

# Boundary: this task did NOT duplicate the sibling tasks' content.
# S1 owns the rules.toml schema field table + §9 example + CLI flag reference.
grep -c '\[\[layer_rules\]\]\|\[\[callback_rules\]\]' docs/qmk-integration.md
#   EXPECT: the single illustrative [[callback_rules]] snippet (1) — NOT the full
#   §9 example (which has 2 layer_rules + 2 callback_rules + a [host] block).
grep -c 'disable_firmware_config' docs/qmk-integration.md
#   EXPECT: a brief mention in stack/replace prose linking OUT to configuration —
#   NOT the full field table (which lives in configuration.md / S1).
grep -c 'llms_full' docs/qmk-integration.md            # 0 — S4 owns regeneration
grep -c -- '--validate-rules\|--list-callbacks' docs/qmk-integration.md
#   EXPECT: 0 or a passing mention — the CLI flag REFERENCE is S1 (configuration.md);
#   this page may name --list-callbacks when explaining the registry, but must link
#   to /configuration for the full flag table.
git diff --name-only                                    # ONLY docs/qmk-integration.md
```

## Final Validation Checklist

### Technical Validation
- [ ] `git diff --stat` = `docs/qmk-integration.md` ONLY (scope gate).
- [ ] ```c / ```toml fences balanced file-wide (Level 1).
- [ ] All `{{ site.baseurl }}/slug` cross-links well-formed; external repo links
      use the correct hyphen/underscore form (Level 1).
- [ ] (If run) `bundle exec jekyll build` reports no Liquid/fence errors (Level 3).

### Feature Validation (contract fidelity — item CONTRACT points 1–5)
- [ ] New `## Host-Side Rules (Optional): Moving Window Rules to the Desktop` H2
      inserted after `## About RAW_USAGE_PAGE / RAW_USAGE_ID`, before
      `## Testing Your Integration`. [CONTRACT 1,2,3,4,5 — container]
- [ ] Intro blockquote: opt-in; `proto_ver == 2` + `APPLY_HOST_CONTEXT` required;
      legacy/disconnected ⇒ silent string-only fallback; board rules unchanged. [CONTRACT 1]
- [ ] **Three-repo architecture** subsection: table naming `qmkonnect`,
      `qmk_notifier` (underscore), `qmk-notifier` (hyphen), with roles + the
      naming-hazard note. [CONTRACT 1]
- [ ] **`DEFINE_HOST_CALLBACKS`** subsection: canonical `mute` example
      (`{ name, on_enable, on_disable }`); `on_disable` may be `NULL`; `id` =
      array index; omit ⇒ byte-for-byte today; no `rules.mk` change. [CONTRACT 2]
- [ ] **Migration** subsection: the 4 steps (expose callbacks → layer rule →
      callback rule → iterate without reflashing) with the accurate "why remove"
      rationale; one illustrative `[[callback_rules]]` snippet; **link to**
      `{{ site.baseurl }}/configuration` for the full schema + CLI flags. [CONTRACT 3,5]
- [ ] **Stack vs. replace** subsection: two planes; stack = string-first then host
      context; replace = context-only (`clear_board`); per-window AND decision;
      host layers ≥ 224; no-match clears. [CONTRACT 4]
- [ ] Existing sections (Overview, Steps 1–5, RAW_USAGE_PAGE, Testing, Common
      Issues, Next Steps) unchanged except optional pointer one-liners. [scope]

### Code Quality Validation (documentation)
- [ ] `DEFINE_HOST_CALLBACKS` example matches the firmware README byte-for-byte.
- [ ] Migration steps + rationale match `HOST_RULES.md` §10 faithfully.
- [ ] Stack/replace prose matches `HOST_RULES.md` §4 / firmware README.
- [ ] The illustrative `[[callback_rules]]` snippet is valid against the LANDED
      `src/core/rules.rs` schema (TOML key `match`; 2-element array form).
- [ ] Markdown table / fence / link formatting consistent with the rest of the file.
- [ ] No duplication of S1 (schema/CLI) / S3 (recipes) / S4 (llms_full.txt) —
      only cross-links out.

### Documentation & Deployment
- [ ] Plain-language explanations (esp. stack/replace, migration rationale) for a
      non-spec-reader audience.
- [ ] Cross-links point at the right sibling pages (configuration for schema/CLI).
- [ ] No mention of internal pipeline state (this is a doc; the feature is locked).

---

## Anti-Patterns to Avoid

- ❌ Don't **gate this doc on S1 landing.** P1–P5 are landed (Complete); the only
      not-yet-landed dependency is the sibling DOC S1 (configuration.md). Treat
      S1's PRP as a contract, link to `/configuration`, and describe the locked
      feature. Do NOT duplicate S1's schema (G1/G2).
- ❌ Don't **duplicate the `rules.toml` schema** (field table, §9 annotated
      example, per-OS file-location table, CLI flag reference). Those are owned by
      S1 (`docs/configuration.md`). LINK out; show only the single illustrative
      `[[callback_rules]]` snippet that makes a migration step concrete (G2).
- ❌ Don't **invent a `DEFINE_HOST_CALLBACKS` struct**. Copy the canonical `mute`
      example from the firmware README (`{ name, on_enable, on_disable }`,
      `on_disable` may be `NULL`, `id` = array index). The pattern is NOT a field
      of the registry (it moves to `rules.toml`) (G5).
- ❌ Don't **misstate the migration rationale**. Removing a rule from the firmware
      macro is to avoid *confusion* (layers: two trackers drive the same layer) or
      *double-firing* (callbacks are additive), NOT because the board rule
      "breaks" or "stops working" — it keeps working and conflicts (G6).
- ❌ Don't **duplicate the sibling tasks' content** — recipes are S3
      (examples.md/troubleshooting.md), `llms_full.txt` regeneration is S4. LINK
      out; don't reproduce (G2/G3).
- ❌ Don't **regenerate `docs/llms_full.txt`**. It's a generated concatenation
      owned by P6.M1.T1.S4, regenerated after all doc edits land (G3).
- ❌ Don't **edit the existing sections** (Overview, Steps 1–5, RAW_USAGE_PAGE,
      Testing, Common Issues, Next Steps) beyond optional trailing pointer
      one-liners. The new content is INSERTED, not folded into existing prose.
- ❌ Don't **confuse the hyphen and underscore repo names**. `qmk-notifier`
      (hyphen) = firmware C module; `qmk_notifier` (underscore) = Rust transport
      crate. Use the correct URL form for each; include the naming-hazard note (G4).
- ❌ Don't use **absolute URLs or `.md` extensions** for internal links. Use
      `{{ site.baseurl }}/slug` to match every existing link in the file (G7).
- ❌ Don't use **unlabeled code fences**. Use ```c / ```toml / ```bash (rouge
      highlights them), matching the existing page (G7).
- ❌ Don't **edit any file other than `docs/qmk-integration.md`** — no Rust, no
      Cargo, no other docs, no spec, no plan files (except this item's own
      PRP/research).

---

## Confidence Score: 9/10

This is a **well-bounded documentation task** with authoritative, locally
available sources: the locked **spec** (`spec/HOST_RULES.md` §2/§4/§6/§10 — the
verbatim 4-step migration, the three-repo table, the coexistence model, the
registry struct), the **canonical firmware README**
(`/home/dustin/projects/qmk-notifier/README.md` — the tested `DEFINE_HOST_CALLBACKS`
`mute` example + the stack/replace prose to mirror), the **LANDED** schema
(`src/core/rules.rs`, P3.M1 Complete — verified structs, for cross-link/snippet
accuracy), and the exact current `docs/qmk-integration.md` skeleton + insertion
point + cross-link conventions (research §1). The boundary with S1
(schema/CLI — link out, don't duplicate), S3 (recipes), and S4 (`llms_full.txt`
regen) is explicit and contractually confirmed in the S1 PRP. With P1–P5 landed,
there are no code blockers at all. The 1-point reservation is the **kramdown
heading-anchor** for the new section (S1 links to `/qmk-integration` the *page*,
so no same-page anchor is strictly required, but the optional Step 4 / Next Steps
pointers depend on a correct `#host-side-rules-…` anchor — best confirmed by one
local Jekyll render, Level 3); a wrong anchor only breaks an optional same-page
jump, trivially fixed. Scope is cleanly one file; the anti-patterns guard
against the two most likely slips (duplicating S1's schema, and misstating the
migration rationale).