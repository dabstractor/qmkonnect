# Research Notes — P1.M1.T1.S1: Remove mise/asdf sections from docs/installation.md

> Doc-only deletion in `docs/installation.md` (3 blocks). llms_full.txt regen = S2.
> No code change.

## 1. Verified current state (`docs/installation.md`, 498 lines)

`grep -in 'mise\|asdf'` → **12 hits**, in 3 clusters. `grep -in 'asdf-qmkonnect'` → **4 hits**.
All confined to the 3 blocks below (no other mise/asdf content in the file).

### BLOCK 1 — intro paragraph (audit lines 29–32)
Preceded by platform overview table (23–27) + blank (28). Followed by `## Windows` (33).
```
29: **mise / asdf** are cross-platform version managers that install the prebuilt release binary:
30: **Linux** (full app) and **macOS** (**CLI only — no menu-bar tray**); not available on Windows.
31: See the per-platform sections.
32: (blank)
```
Delete 29–32 (content + trailing blank); keep blank 28 ⇒ table → blank → `## Windows`.

### BLOCK 2 — Linux subsection (audit lines 289–301)
Preceded by Nix paragraph (ends 287) + blank (288). Followed by `**.deb (Debian / Ubuntu)**` (302).
```
289: **mise / asdf** — cross-platform version managers. The same `asdf-qmkonnect` plugin serves both
290: (mise runs asdf plugin scripts unchanged). **Linux is fully supported** — install the binary, then
291: run the one-time udev/systemd setup the plugin documents:
292: (blank)
293: ```bash
294: # asdf:
295: asdf plugin add qmkonnect https://github.com/dabstractor/asdf-qmkonnect
296: asdf install qmkonnect latest
297: # mise:
298: mise plugin add qmkonnect https://github.com/dabstractor/asdf-qmkonnect
299: mise install qmkonnect@latest
300: ```
301: (blank)
```
Delete 289–301 (intro + fence + commands + trailing blank); keep blank 288 ⇒ Nix → blank → `.deb`.
Contains 3 of the 4 broken `asdf-qmkonnect` links.

### BLOCK 3 — macOS subsection (audit lines 367–376)
Preceded by Homebrew uninstall paragraph (ends 365) + blank (366). Followed by `### Launch at login` (377).
```
367: **mise / asdf — CLI only (no menu-bar tray).** These install the raw Mach-O binary from the DMG,
368: which runs CLI flags (`--help`, `--list-callbacks`, `-r`, …) but **not** the menu-bar tray/icon —
369: that needs the full `.app` bundle. For the complete macOS app, use the **Homebrew cask** above or
370: the **direct DMG** instead:
371: (blank)
372: ```bash
373: asdf plugin add qmkonnect https://github.com/dabstractor/asdf-qmkonnect
374: asdf install qmkonnect latest        # CLI only — no menu-bar app
375: ```
376: (blank)
```
Delete 367–376 (intro + fence + commands + trailing blank); keep blank 366 ⇒ Homebrew → blank → `### Launch at login`.
Contains the 4th broken `asdf-qmkonnect` link.

## 2. The deletion rule (deterministic spacing)

For each block: delete the content lines + the **trailing** blank line; keep the **preceding**
blank line as the single separator. Yields exactly one blank at each junction:
- table-row (27) → blank (28) → `## Windows`
- Nix-para (287) → blank (288) → `**.deb...**`
- Homebrew-para (365) → blank (366) → `### Launch at login`

## 3. Why (authoritative decision, already synced in spec)

- `spec/PACKAGING.md` §6.4 "mise / asdf — NOT a channel (category mismatch)": no autostart; version-
  switching meaningless for a single-instance daemon; updates re-wire autostart. `packaging/asdf/` removed.
- `spec/PRD.md` F15 (and §2.1 item 6, §5): channel list = AUR · .deb/.rpm · Homebrew · Scoop · Winget ·
  Nix; mise/asdf explicitly excluded.
- The user-facing `docs/installation.md` is the **sole residual drift** (audit §3 confirms every other
  file — PACKAGING.md, PRD.md, README.md, release.yml, src/*.rs, other docs/*.md — is already clean).

## 4. llms_full.txt dependency (S2, not S1)

`docs/llms_full.txt` is GENERATED by `docs/generate_llms_full.sh` (concatenates README → index →
installation → qmk-integration → configuration → usage → examples → troubleshooting, stripping Jekyll
front-matter). The audit §2 maps its stale mise/asdf lines to installation.md:
- line 490 ← installation.md §29 (BLOCK 1)
- lines 750–760 ← installation.md §289–301 (BLOCK 2)
- lines 828–835 ← installation.md §367–376 (BLOCK 3)
- lines 157, 160 ← old README snapshot (README already clean ⇒ regen fixes these too)

**Ordering:** fix installation.md (S1) FIRST, then regenerate llms_full.txt (S2). Regenerating before
the source fix would leave the installation.md-derived lines stale.

## 5. Validation gates (deterministic)

- `grep -in 'mise\|asdf' docs/installation.md` → **zero** hits.
- `grep -in 'asdf-qmkonnect' docs/installation.md` → **zero** hits.
- `git diff --stat -- docs/installation.md` → 1 file, deletions only.
- `awk '/^$/{n++; if(n>=2) print NR": DOUBLE-BLANK"; next} {n=0}'` → no double-blanks introduced.
- `grep -c '```'` → even (the 2 deleted ```bash fences were balanced: −2 openers, −2 closers).
- Neighbors present in order: `## Windows`, `**.deb (Debian / Ubuntu)**`, `### Launch at login`.
- Platform table (23–27) byte-identical.
- `git status --short` → only `M docs/installation.md` (NOT llms_full.txt, NOT spec/*).

## 6. Scope boundaries (NOT this subtask)

- `docs/llms_full.txt` regeneration → P1.M1.T1.S2 (run `bash docs/generate_llms_full.sh` after this edit).
- README.md / spec doc re-sync verification → P1.M1.T2.S1 (audit §3 says they're already clean).
- Do NOT edit: `spec/PACKAGING.md`, `spec/PRD.md` (read-only justification, already synced); the platform
  overview table (23–27); Nix/.deb/.rpm/Homebrew/Scoop/Winget/AUR sections.